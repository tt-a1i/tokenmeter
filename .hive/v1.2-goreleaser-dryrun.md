# v1.2 GoReleaser Snapshot Dry-Run

> Reviewer: 李诫  ·  时间: 2026-05-25  ·  HEAD: `53e221f`

## GoReleaser Availability

- local binary: `/opt/homebrew/bin/goreleaser`
- version: `2.15.3`
- platform: `darwin/arm64`

## goreleaser snapshot

Command:

```bash
goreleaser release --snapshot --clean --skip=publish 2>&1 | tee /tmp/v12-goreleaser-snapshot.log
```

- exit code: `0`
- snapshot version: `1.1.0-SNAPSHOT-53e221f`
- binaries generated: `6`
- archives generated: `6`
- checksums: yes (`dist/checksums.txt`)
- homebrew cask generated: yes (`dist/homebrew/Casks/tokenmeter.rb`)

Generated binaries:

| Target | Binary |
|---|---|
| `darwin_amd64_v1` | `tm` |
| `darwin_arm64_v8.0` | `tm` |
| `linux_amd64_v1` | `tm` |
| `linux_arm64_v8.0` | `tm` |
| `windows_amd64_v1` | `tm.exe` |
| `windows_arm64_v8.0` | `tm.exe` |

Generated archives:

| Archive | Result |
|---|---|
| `tokenmeter_darwin_amd64.tar.gz` | generated |
| `tokenmeter_darwin_arm64.tar.gz` | generated |
| `tokenmeter_linux_amd64.tar.gz` | generated |
| `tokenmeter_linux_arm64.tar.gz` | generated |
| `tokenmeter_windows_amd64.zip` | generated |
| `tokenmeter_windows_arm64.zip` | generated |

## release.yml audit

There is no separate `.github/workflows/release.yml`; release is embedded in `.github/workflows/ci.yml`.

| Item | Result | Notes |
|---|---|---|
| trigger | ✅ | `push` to `main`, `push` tags matching `v*`, and PRs to `main`. |
| release gate | ✅ | `release` job runs only when `startsWith(github.ref, 'refs/tags/v')`. |
| test dependency | ✅ | `release` needs the matrix `test` job. |
| Go version | ✅ | `actions/setup-go@v6` with `go-version: "1.24"`. |
| GoReleaser action | ✅ | `goreleaser/goreleaser-action@v6`, `version: "~> v2"`. |
| GitHub release token | ✅ | `GITHUB_TOKEN` with `contents: write`. |
| Homebrew token | ✅ | `HOMEBREW_TAP_GITHUB_TOKEN` optional secret. |
| skip_upload graceful | ✅ | CI appends `--skip=homebrew` when `HOMEBREW_TAP_GITHUB_TOKEN` is empty; `.goreleaser.yml` also templates `skip_upload` for the cask. |
| artifact upload | ✅ | GoReleaser handles GitHub release artifacts automatically in tag release mode. |

## archive content

Checked with:

```bash
tar tzf dist/tokenmeter_linux_amd64.tar.gz
```

| File | Result |
|---|---|
| `tm` binary | ✅ |
| `LICENSE` | ✅ |
| `README.md` | ✅ |
| `README_EN.md` | ✅ |
| `CHANGELOG.md` | ✅ |

## Ship-blocker summary

- New GoReleaser/release-CI blockers: `0`
- Existing unrelated release blocker remains outside this dry-run scope: live `tm pricing refresh` LiteLLM JSON schema mismatch from final readiness check #2.

## Cleanup

- `dist/` was removed after inspection.
- `/tmp/v12-goreleaser-snapshot.log` retains the full dry-run output for local review.

## Conclusion

✅ Release CI dry-run passes locally. GoReleaser configuration is ready to tag-trigger release after the existing LiteLLM pricing blocker is fixed or explicitly waived.

No user intervention is required for release config before tagging.
