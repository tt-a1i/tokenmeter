# v1.2 Pre-release Verification Report

> Author: 李诫  ·  时间: 2026-05-25
> Compared with: clean HEAD `62ea6c3` (`Add v1.2 smoke coverage`)

## Build / Test / Vet / Race

| Check | Result | Notes |
|---|---|---|
| `go build -o /tmp/tm-v12-binary ./cmd/tm` | ✅ | Binary built from clean `/tmp/tm-v12-verify` worktree. |
| `go test ./...` | ❌ | Fails in `internal/render`: `TestRenderBlocksTokenLimitColorsStatuses` expected ANSI `OK/WARN/ALERT`, output was plain text. Full output: `/tmp/v12-test-output.txt`. |
| `go vet ./...` | ✅ | No warnings. Full output: `/tmp/v12-vet-output.txt`. |
| `go test -race ./...` | ❌ | No data race reported, but overall command fails on same `internal/render` ANSI color test. Full output: `/tmp/v12-race-output.txt`. |

## Help Discovery Check

| Check | Result | Notes |
|---|---|---|
| `blocks --help` contains `--token-limit` | ✅ | `/tmp/help-blocks.txt` |
| `analyze --help` contains `--tool-errors` | ✅ | `/tmp/help-analyze.txt` |
| `analyze --help` contains `--file-churn` | ✅ | `/tmp/help-analyze.txt` |
| `search --help` contains `tool:NAME` | ✅ | `/tmp/help-search.txt` |
| `statusline --help` contains `burn-rate-display` | ✅ | `/tmp/help-statusline.txt` |
| `daily --help` contains `--instances` | ✅ | `/tmp/help-daily.txt` |
| top-level `--help` contains `tm config` | ⚠️ | Top help contains `config <subcommand>` but not literal `tm config`. |

Executed help commands:

```bash
/tmp/tm-v12-binary --help > /tmp/help-top.txt
/tmp/tm-v12-binary daily --help > /tmp/help-daily.txt
/tmp/tm-v12-binary analyze --help > /tmp/help-analyze.txt
/tmp/tm-v12-binary search --help > /tmp/help-search.txt
/tmp/tm-v12-binary statusline --help > /tmp/help-statusline.txt
/tmp/tm-v12-binary blocks --help > /tmp/help-blocks.txt
/tmp/tm-v12-binary config --help > /tmp/help-config.txt
/tmp/tm-v12-binary pricing --help > /tmp/help-pricing.txt
```

## Makefile Audit

| Target | Result | Notes |
|---|---|---|
| `build` | ✅ | Builds `./cmd/tm` with version ldflag. |
| `install` | ✅ | Installs to `$(go env GOPATH)/bin/tm` and best-effort ad-hoc codesign. |
| `test` | ✅ | Runs `go test -cover ./...`. |
| `test-e2e` | ✅ | Runs `go test ./cmd/tm/... -run TestE2EV12Smoke -count=1`. |
| `lint` | ✅ | Runs `golangci-lint run --timeout=5m`. |
| `vet` | ✅ | Runs `go vet ./...`. |
| `race` | ✅ | Runs `go test ./... -race -count=5 -timeout=120s`. |
| `release-check` | ❌ | Missing target for build + test + vet + race + help discovery gate. |
| `test-smoke` | ⚠️ | No separate `test-smoke`; current smoke coverage is under `test-e2e`. |

## goreleaser Audit

Config file: `.goreleaser.yml`

### ✅ Ready

- Uses GoReleaser v2 schema: `version: 2`.
- Builds `./cmd/tm` to binary `tm`.
- Uses `CGO_ENABLED=0`.
- Targets `darwin`, `linux`, `windows` on `amd64` and `arm64`.
- Injects version with `-X main.version={{ .Version }}`.
- Archives use `tar.gz`, with Windows `zip` override.
- Checksum file configured as `checksums.txt`.
- Changelog auto-generated from git history with `sort: asc`.
- Homebrew cask uses `tt-a1i/homebrew-tap`, `binaries: ["tm"]`, and GoReleaser v2 `homebrew_casks`.
- Homebrew upload degrades when `HOMEBREW_TAP_GITHUB_TOKEN` is missing via `skip_upload` template.
- CI release workflow uses `goreleaser/goreleaser-action@v6` and `version: "~> v2"`.

### ⚠️ Need Update / Review

- No explicit `release` section in `.goreleaser.yml`; GitHub release upload relies on GoReleaser defaults.
- No archive file list such as `README`, `LICENSE`, or completions in `archives`.
- Go version is controlled by CI (`1.24`) and `go.mod` (`1.24.2`), not by GoReleaser config itself.
- `release.yml` is not present as a separate workflow; release is embedded in `.github/workflows/ci.yml`.

### ❌ Missing

- No `release-check` Makefile target before tag.
- No explicit v1.2 changelog/release notes gate; GoReleaser changelog is automatic only.

## Final Readiness Check #2

> Compared with: clean HEAD `6f2aa52` (`Add advanced guides for v1.2 beyond features`)
> Note: previous render ANSI blocker found at `4e86ef9` was resolved by `6aa6e1a`; clean verification below ran after that fix.

### Build / Test / Vet / Race / E2E

| Check | Result | Exit Code | Notes |
|---|---|---:|---|
| `go build -o /tmp/tm-v12-final ./cmd/tm` | ✅ | 0 | Built from clean `/tmp/tm-v12-final-verify` worktree. Output: `/tmp/v12-final-build.txt`. |
| `go test ./...` | ✅ | 0 | Full suite passed. Output: `/tmp/v12-final-test.txt`. |
| `go vet ./...` | ✅ | 0 | No warnings. Output: `/tmp/v12-final-vet.txt`. |
| `go test -race ./...` | ✅ | 0 | Full suite passed under race detector. Output: `/tmp/v12-final-race.txt`. |
| `make test-e2e` | ✅ | 0 | `TestE2EV12Smoke` passed. Output: `/tmp/v12-final-e2e.txt`. |

### Help Discovery Check

| Check | Result | Notes |
|---|---|---|
| `blocks --help` contains `--token-limit` | ✅ | `/tmp/help-blocks-final.txt` |
| `analyze --help` contains `--tool-errors` | ✅ | `/tmp/help-analyze-final.txt` |
| `analyze --help` contains `--file-churn` | ✅ | `/tmp/help-analyze-final.txt` |
| `search --help` contains `tool:` | ✅ | `/tmp/help-search-final.txt` |
| `statusline --help` contains `burn-rate-display` | ✅ | `/tmp/help-statusline-final.txt` |
| `daily --help` contains `--instances` | ✅ | `/tmp/help-daily-final.txt` |
| `daily --help` contains `--project-aliases` | ✅ | `/tmp/help-daily-final.txt` |
| `daily --help` contains `--compact` | ✅ | `/tmp/help-daily-final.txt` |
| top-level `--help` contains `config` | ✅ | `/tmp/help-top-final.txt` |
| top-level `--help` contains `pricing` | ✅ | `/tmp/help-top-final.txt` |

### Binary Smoke Check

| Command | Result | Exit Code | Notes |
|---|---|---:|---|
| `/tmp/tm-v12-final pricing refresh` | ❌ | 1 | Fails parsing current LiteLLM pricing: `json: cannot unmarshal string into Go struct field liteLLMEntry.max_input_tokens of type int64`. Output: `/tmp/v12-final-pricing-refresh.txt`. |
| `TOKENMETER_HOME=$(mktemp -d) /tmp/tm-v12-final config init` | ✅ | 0 | Command succeeded, but output path was `/Users/admin/.tokenmeter/config.json`, not the temp `TOKENMETER_HOME`. Output: `/tmp/v12-final-config-init.txt`. |
| `TOKENMETER_HOME=$(mktemp -d) /tmp/tm-v12-final config show` | ✅ | 0 | Printed default config. Output: `/tmp/v12-final-config-show.txt`. |
| `TOKENMETER_HOME=$(mktemp -d) /tmp/tm-v12-final config path` | ✅ | 0 | Command succeeded, but output path was `/Users/admin/.tokenmeter/config.json`, not the temp `TOKENMETER_HOME`. Output: `/tmp/v12-final-config-path.txt`. |
| `/tmp/tm-v12-final version` | ✅ | 0 | Printed `tm vdev`. Output: `/tmp/v12-final-version.txt`. |

### GoReleaser Final Review

The GoReleaser configuration is consistent with the previous audit and is not itself a v1.2 blocker:

- ✅ `builds` targets `./cmd/tm`, binary `tm`, `darwin/linux/windows`, `amd64/arm64`, and injects version with ldflags.
- ✅ `archives`, `checksum`, `changelog`, and `homebrew_casks` are present.
- ✅ Release is embedded in `.github/workflows/ci.yml` and uses GoReleaser v2 on tag pushes.
- ⚠️ v1.3 follow-up: add an explicit `release` section if upload behavior should be declarative.
- ⚠️ v1.3 follow-up: include `README`, `LICENSE`, and completions in archives.
- ⚠️ v1.3 follow-up: Go version is controlled by CI/go.mod, not by GoReleaser config.

## Blocker / Recommended Action

- [Resolved] `internal/render TestRenderBlocksTokenLimitColorsStatuses` blocker from `4e86ef9`; fixed by `6aa6e1a` and covered by final clean `go test ./...` plus `go test -race ./...`.
- [B] Fix `tm pricing refresh` against current LiteLLM pricing data. Current binary exits 1 on `max_input_tokens` when upstream sends a JSON string.
- [R] Confirm whether `TOKENMETER_HOME` is intended to affect `tm config init/show/path`; current binary exits 0 but writes/prints `/Users/admin/.tokenmeter/config.json` during the temp-home smoke check.
- [R] Add Makefile `release-check` target: build + `go test ./...` + `go vet ./...` + `go test -race ./...` + help discovery grep checks.
- [R] Decide whether top-level help should include literal `tm config` to satisfy the current pre-release grep, or update the release checklist to accept `config <subcommand>`.
- [F] Consider adding `README`, `LICENSE`, and generated completions to GoReleaser archives.
- [F] Consider an explicit `.goreleaser.yml release` section if the team wants upload behavior to be declarative rather than default-driven.

## Conclusion

Do **not** tag `v1.2.0` yet.

History:

- `4e86ef9` verification found 1 blocker in `internal/render` ANSI color tests.
- `6aa6e1a` resolved that blocker; final clean verification at `6f2aa52` passes build, test, vet, race, and e2e.

Current final check #2 result is **NOT READY** because `/tmp/tm-v12-final pricing refresh` exits 1 against live LiteLLM pricing data. The failure is outside the test suite and should be fixed or explicitly waived before tagging.
