# v1.2.0 Ship-Day Checklist

> Last updated: 2026-05-25  
> Source of truth: `.hive/v1.2-*.md` reports + `git log v1.1.0..HEAD`  
> Final sweep HEAD: `770d243` (`Add v1.2 final acceptance review`)

## Overall Status

✅ **READY TO TAG** — final readiness sweep passed at `770d243`; v1.2.0 tag is safe to create.

## Done

- [x] All 4 beyond-ccusage features:
  - `5ec2cb3` — tool error analysis
  - `caaa4ce` — file churn analysis
  - `ae5f4bf` + `4c415ea` — anomaly webhook events + cooldown follow-up
  - `b6d00ad` — FTS5 advanced search query filters
- [x] All 9 ccusage-aligned user-facing features:
  - `9e78818` — OpenCode SQLite loader + Droid sidecar JSONL model fallback
  - `e0ba1c4` — LiteLLM runtime pricing + Codex speed tiers
  - `b10272d` — responsive tables + statusline controls
  - `992ebd1` — `tm blocks --token-limit`
  - `7215719` — project aliases + `--instances`
  - `2c8b307` + `892de23` + `4c415ea` — unified config model, defaults, and `*bool` override semantics
- [x] Documentation foundation:
  - `585b343` — VitePress documentation site skeleton
  - `97e2734` — expanded source documentation
  - `e13a38c` — CLI default behavior docs fix
  - `432baab` — front-page docs refresh for Wave 2
- [x] Wave 2 review:
  - `6df4dc9` — Wave 2 acceptance review
  - `5ef334f` — 4 P1 review findings fixed
- [x] Wave 3 review:
  - `2ba4bd8` — Wave 3 acceptance review
  - `3b34305` — deterministic project aliases + leading global flags
  - `bd28965` — help sync for analyze and project aliases
  - `1dbd7df` — README quickstart + unified config docs refresh
- [x] e2e smoke:
  - `62ea6c3` — v1.2 smoke coverage
  - `1796905` — tightened smoke expectations; final state recorded as 17 pass / 1 skip in review history
- [x] BLOCKER #1 ANSI render test resolved:
  - `4e86ef9` — prerelease verification found clean HEAD render ANSI failure
  - `6aa6e1a` — render color test environment fixed
- [x] Final readiness check #2:
  - `4701102` — build/test/vet/race/e2e all passed after ANSI fix, then found live LiteLLM blocker
- [x] TOKENMETER_HOME env fix:
  - `53e221f` — `TOKENMETER_HOME` now overrides appdir root for `tm config path/init/show`
- [x] Project-scoped analyze insights:
  - `12e8d93` — analyze insights scoped to current project
  - `5b71714` — docs synced with project scoping
- [x] CHANGELOG and release notes:
  - `7b16df5` — v1.2.0 changelog + release notes draft
  - `7a2c105` — changelog cross-validation
  - `a0abdf1` — changelog refined from cross-validation findings
- [x] Advanced guides:
  - `6f2aa52` — 4 advanced guides, 1961 lines total
- [x] Release announcement package:
  - `0bf7069` — GitHub release draft + social posts
- [x] GoReleaser release-CI dry-run:
  - `76ef934` — snapshot dry-run passed; 6 binaries / 6 archives / checksums / Homebrew cask; 0 new release-CI blocker
- [x] BLOCKER #2 LiteLLM live pricing schema resolved:
  - `718d2e1` — accepts string or number fields in LiteLLM pricing schema
  - Spot check: `go run ./cmd/tm pricing refresh` returned exit 0 with `pricing cache refreshed`
- [x] Pricing closed-loop e2e verification:
  - `2b38b20` — verifies v1.2 pricing e2e flow
- [x] Final acceptance review:
  - `770d243` — final acceptance review added
- [x] Final readiness sweep #3:
  - `770d243` — build/test/vet/race/e2e/pricing/goreleaser all passed

## In Progress

- None known from the v1.2 ship checklist perspective.

## Blockers

- None known after `718d2e1`.
- Historical blocker: `tm pricing refresh` failed on live LiteLLM data with `max_input_tokens` string vs int64. It was first recorded in `4701102` and resolved by `718d2e1`.

## Recommended, Non-Blocking

- Add shell completions to GoReleaser archives.
- Add an explicit `.goreleaser.yml` `release` section if upload behavior should be declarative instead of default-driven.
- Keep Go version controlled by CI/go.mod unless the team wants an additional release-config assertion.
- Consider adding a Makefile `release-check` target for build + test + vet + race + help discovery.
- Add project filtering to search if the `12e8d93` analyze scoping pattern should extend to query results.
- Persist anomaly cooldown if daemon restarts causing duplicate alerts becomes a real operational issue.
- Apply `sources.<name>` unified config schema to adapter-specific defaults.
- Consider a verbose diagnostic mode for OpenCode SQLite read errors that currently fall back silently.

## Ready-to-execute

### Tag commands

From `.hive/v1.2-tag-commands.md`; run only after final verification passes.

```bash
# Confirm local main is current.
git fetch origin
git checkout main
git pull --ff-only origin main
git log -1 --oneline  # should be the final verified commit

# Confirm the working tree is clean except known local-only files.
git status --short  # should only show known .hive/ or local scratch files, if any
```

```bash
git tag -a v1.2.0 -m "Release v1.2.0 — see CHANGELOG.md"
git push origin v1.2.0
```

```bash
# Check that the tag-triggered release workflow started.
gh run list --workflow=CI --limit=5
gh run list --workflow=release.yml --limit=3 2>/dev/null || true
gh release view v1.2.0
```

```bash
# Rollback only if release CI fails and the tag must be re-issued after a fix.
git tag -d v1.2.0
git push origin :v1.2.0
# Fix the issue, then create and push the tag again.
```

### Post-tag actions

- GitHub release: copy from `.hive/v1.2-github-release.md`.
- Social posts: copy from `.hive/v1.2-social-posts.md`.
- Review caveat: social draft uses "Shipped"; publish only on release day after tag/release succeeds.

## Stats

- v1.2 commits from `v1.1.0` to final sweep HEAD: `40`
- Workers contributed: 李诫 / 颜真卿 / 公孙弘
- Clean readiness at `4701102`: build/test/vet/race/e2e all exit 0, then live pricing smoke found BLOCKER #2
- GoReleaser snapshot at `76ef934`: exit 0, 6 binaries, 6 archives, checksums, Homebrew cask
- GoReleaser snapshot build time: 17s
- Archive contents checked: `tm`, `LICENSE`, `README.md`, `README_EN.md`, `CHANGELOG.md`
- Commit hash validation for checklist references: all present; missing list is empty

## Last Verification

Final readiness sweep #3 passed from clean worktree `/tmp/tm-v12-ship` at `770d243`.

| Check | Exit Code | Notes |
|---|---:|---|
| `go build -o /tmp/tm-v12-ship-binary ./cmd/tm` | 0 | Output: `/tmp/v12-ship-build.txt` |
| `go test ./...` | 0 | Output: `/tmp/v12-ship-test.txt` |
| `go vet ./...` | 0 | Output: `/tmp/v12-ship-vet.txt` |
| `go test -race ./...` | 0 | Output: `/tmp/v12-ship-race.txt` |
| `make test-e2e` | 0 | Output: `/tmp/v12-ship-e2e.txt` |
| `TOKENMETER_HOME=$(mktemp -d) /tmp/tm-v12-ship-binary pricing refresh` | 0 | Output: `pricing cache refreshed`; full output: `/tmp/v12-ship-pricing.txt` |
| `goreleaser release --snapshot --clean --skip=publish` | 0 | Output: `/tmp/v12-ship-goreleaser.txt`; `dist/` had 17 entries before cleanup |

Cleanup completed:

- `/tmp/tm-v12-ship` worktree removed
- `/tmp/tm-v12-ship-binary` removed

## What To Do Next

### Ship now

1. Execute the tag commands below from repo root.
2. Confirm the tag-triggered CI release starts and succeeds.
3. Publish GitHub release and social posts from the `.hive` drafts.

### If a newer commit lands before tagging

1. Re-run the final readiness sweep on the newer HEAD.
2. Update this checklist with the new HEAD and exit codes.
3. Tag only after the newer sweep exits 0.

### If ship is deferred

1. Leave current dev state on `main`.
2. Do not create `v1.2.0` tag.
3. Keep `770d243` or later as the base for the next release attempt.

## Immediate Tag Commands

```bash
git fetch origin
git checkout main
git pull --ff-only origin main
git log -1 --oneline  # should be 770d243 or later
git status --short

git tag -a v1.2.0 -m "Release v1.2.0 — see CHANGELOG.md"
git push origin v1.2.0

gh run list --workflow=CI --limit=5
gh release view v1.2.0
```
