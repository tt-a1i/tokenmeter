# v1.2.0 Final Acceptance Review

> Reviewer: 公孙弘 · 时间: 2026-05-25  
> Scope: 16 requested commits (3b34305 → 5b71714) + observed HEAD extensions 4701102 / 718d2e1  
> Wave 2 / 3 review 已经 PASS；本次为 v1.2 ship 前最终一道。

## 总评

v1.2 收尾段整体可以 ACCEPT。代码类修复覆盖了 alias ordering、leading global flags、project-scoped insights、TOKENMETER_HOME、LiteLLM schema 兼容和 v1.2 smoke；文档类提交也把 README、CHANGELOG、release draft、advanced guides、project scope 变更同步到位。最终在独立 worktree 的 HEAD 上跑了 `go test ./...`、`go vet ./...`、`go test -race ./...`、`make test-e2e`，四项 exit code 均为 0。

本轮没有发现 P0/P1 ship blocker。发现 2 个 P2 级别的边界/后续项：一个是 top-level global flag 与 implicit daily 的组合仍不可用；另一个是 explicit `--project NAME` 使用 basename 匹配，极端情况下会合并同名 checkout。这两个都有明确 workaround，不建议阻塞 v1.2 tag。

## 逐 commit findings

### 3b34305 李诫 - Fix alias ordering and leading global flags

- ✅ ACCEPT
- 实现核对：`projectalias.Aliases` 从 map 改为有序 `AliasEntry`，JSON object 解析保持 first-wins；`cmd/tm/main.go` 增加 `normalizeTopLevelArgs`，允许 `--config` / `--no-color` / `--offline` 放在显式子命令前。
- 测试核对：覆盖 alias first-wins、重复运行稳定性、`--config` before/after/equals、`--no-color` before subcommand。
- Findings:
  - [P2] `tm --offline` 这类“global flag + implicit daily”仍会被当成未知命令。触发条件：没有显式子命令，只传 top-level global flag；复现：`tm --offline` exit 1，stderr 为 `unknown command: --offline`。证据：`normalizeTopLevelArgs` 在只消费到 globals 且没有遇到 subcommand 时返回原始 args（`cmd/tm/main.go:226-259`）。最小修复建议：当 `normalizeTopLevelArgs` 只收到 global flags 时返回 `append([]string{"daily"}, globals...)`，并加 `TestNormalizeTopLevelArgsOfflineImplicitDaily`。

### bd28965 公孙弘 - Sync help for analyze and project alias flags

- ✅ ACCEPT
- 自审核对：`daily/weekly/monthly/session` help 补齐 `--instances`、`--project-aliases`；`analyze` help 补齐 `--tool-errors`、`--file-churn`、`--all-projects`、`--project NAME`、`--project-aliases JSON|PATH`。
- 测试核对：`cmd/tm/cmd_help_test.go` 有 daily/project alias、analyze tool errors/file churn/project scope 的 presence tests。测试偏字符串存在性，但对 help regression 足够。

### 1dbd7df 公孙弘 - Refresh README quickstart and unified config docs

- ✅ ACCEPT
- 自审核对：README quickstart 移除了 `tm pricing refresh --offline` 作为 happy-path 命令，避免 clean install copy-paste 触发 exit 1；unified-config 文档同步了已生效 scope、`*bool` false override、webhook anomaly cooldown。
- 风险：文档没有 claim `sources.<name>` 已生效，保留为后续项，符合当时代码状态。

### 62ea6c3 颜真卿 - Add v1.2 smoke coverage

- ✅ ACCEPT
- 实现核对：新增 `make test-e2e` target 与 `cmd/tm/e2e_v12_smoke_test.go`，覆盖 v1.2 关键 CLI 行为。
- 测试核对：最终 `make test-e2e` exit 0，说明 smoke suite 可在当前 HEAD 重现。

### 6aa6e1a 李诫 - Fix render color test environment

- ✅ ACCEPT
- 实现核对：修正 render color test 的环境前提，属于此前 BLOCKER 的 targeted fix。
- 验证：最终 `go test ./...` 和 `go test -race ./...` 均通过，说明该 fix 没有继续污染全局颜色环境。

### 4e86ef9 李诫 - Add v1.2 prerelease verification report

- ✅ ACCEPT
- 报告核对：`.hive/v1.2-prerelease-verify.md` 记录 prerelease verification 过程和结果，属于可追溯 release evidence。
- 剩余风险：这是报告提交，不替代本轮最终 HEAD verification；本轮已重新跑四项测试。

### 7b16df5 公孙弘 - Add v1.2.0 changelog and release notes draft

- ✅ ACCEPT
- 自审核对：新增 docs/site changelog、根 CHANGELOG v1.2 章节、release notes draft，并接入 VitePress sidebar。
- 后续修正：a0abdf1 已根据 cross-validation 调整分类、补 commit 引用和 known limitations，本 commit 的初版问题已闭环。

### 1796905 颜真卿 - Tighten v1.2 smoke expectations

- ✅ ACCEPT
- 实现核对：强化 smoke expectation，不只验证命令能跑，还验证输出关键内容。
- 验证：最终 `make test-e2e` exit 0，说明 tightened expectations 在 HEAD 可重现。

### 7a2c105 颜真卿 - Add v1.2 changelog cross validation

- ✅ ACCEPT
- 报告核对：cross-validation 抓到 CHANGELOG 分类和事实同步问题；a0abdf1 已处理 7 项，P2-3 随 12e8d93 / 5b71714 后续同步。
- 剩余风险：作为审查报告提交，无代码风险。

### 6f2aa52 公孙弘 - Add advanced guides for v1.2 beyond features

- ✅ ACCEPT
- 自审核对：4 个 advanced guide 均进入 sidebar；内容覆盖 tool errors、file churn、search DSL、webhook anomaly。`cost:N` 下限 shorthand 在 search guide 中解释为 exact/lower-bound behavior，避免误导。
- 后续同步：12e8d93 改变 analyze scope 后，5b71714 已把 guide 中 whole-database 默认描述改为 current project default。

### a0abdf1 公孙弘 - Refine v1.2 changelog per cross-validation findings

- ✅ ACCEPT
- 自审核对：补齐 1796905 / 4e86ef9 / 432baab / 7b16df5 等引用；把 docs accuracy 从 ccusage parity feature 中拆出；拆分 7215719 与 3b34305；补 tool errors daily sparkline、file churn mode breakdown/hotspots/sparkline。
- 剩余风险：P2-3 file churn global limitation 当时刻意留给 12e8d93 后同步，已由 5b71714 补上。

### 12e8d93 颜真卿 - Scope analyze insights to current project

- ✅ ACCEPT
- 实现核对：`tm analyze --tool-errors` / `--file-churn` 默认 current project scope；新增 `--all-projects`、`--project NAME`、`--project-aliases`；JSON 增 `project_scope`。代码同时覆盖 render JSON 和 storage scoped query。
- 测试核对：`cmd/tm/analyze_test.go` 覆盖 current/all/explicit scope；`internal/storage/*` 覆盖 scoped top tools、patterns、file churn、daily trends。
- Findings:
  - [P2] `--project NAME` 的 fallback 匹配是 `cwd == project || filepath.Base(cwd) == project`（`internal/storage/file_churn.go:286-314`）。如果本地 DB 里有两个不同路径但 basename 相同的 checkout，explicit project 可能合并它们。最小修复建议：v1.3 增加 canonical project key 或要求 `--project-aliases` 明确 disambiguate；短期在 docs 加一句同名 checkout caveat 即可。

### 0bf7069 公孙弘 - Draft v1.2 release announcements

- ✅ ACCEPT
- 自审核对：release announcement / social drafts 使用 MIT license，不再误写 Apache 2.0；`15 source CLIs`、`9 aligned features`、`WebFetch 28.9%`、`MapView.tsx 365 changes` 均来自已记录 commit/report，不含未验证性能数字。
- 注意：发布前如果 tag 不包含 718d2e1，应删除 “LiteLLM fixed” 暗示；当前 HEAD 已包含 718d2e1，可保留。

### 53e221f 李诫 - Honor TOKENMETER_HOME env in appdir resolution

- ✅ ACCEPT
- 实现核对：`internal/appdir.Base()` 支持 `TOKENMETER_HOME`，并让 config path/init/show 跟随该目录。
- 测试核对：新增 appdir/config tests 覆盖 env override、legacy path merge、`config path/init/show`。

### 76ef934 李诫 - Add v1.2 goreleaser snapshot dry-run report

- ✅ ACCEPT
- 报告核对：新增 goreleaser snapshot dry-run report，属于 release readiness evidence。
- 剩余风险：报告不是 CI artifact 本身；tag 前仍建议 release owner 保留实际 dry-run log。

### 5b71714 公孙弘 - Sync analyze insight docs with project scoping

- ✅ ACCEPT
- 自审核对：同步了 project scoped analyze 的 5 处文档面：CHANGELOG、docs/site changelog、tool-error guide、file-churn guide、cli-reference/release-facing docs。`project_scope`、`--all-projects`、`--project NAME`、`--project-aliases` 均可在文档中找到。
- 验证：`rg` 显示 tool-error/file-churn guides 均说明 default `current`、explicit `explicit:<name>`、all `all`。

### 4701102 李诫 - Confirm v1.2 release readiness

- ✅ ACCEPT
- 范围说明：此 commit 不在派单 16 commit 列表中，但存在于 `2ba4bd8..HEAD`，本轮一并看过。
- 报告核对：更新 prerelease verify，并新增 `.hive/v1.2-tag-commands.md`。这是 release operation evidence，不改产品代码。

### 718d2e1 颜真卿 - Accept string or number in LiteLLM pricing schema

- ✅ ACCEPT
- 范围说明：派单里写 LiteLLM blocker pending，但最终 HEAD 已包含此 commit；本轮必须按实际 tag candidate 审。
- 实现核对：新增 `stringOrInt` / `stringOrFloat` JSON unmarshal，兼容 LiteLLM pricing 中数字或字符串字段；`sample_spec` 被跳过；bad numeric string / fractional int 有测试。
- 测试核对：`internal/pricing/litellm_types_test.go` 和 `runtime_test.go` 覆盖真实 sample、string/number mixed schema、错误输入。最终全量测试 green。

## Pending

- LiteLLM JSON schema fix：在派单时 pending，但当前 HEAD 已由 `718d2e1` 修复并通过最终 verification。若 release tag 不包含 `718d2e1`，该 blocker 重新成立；若 tag 基于当前 HEAD，则 pending 为 none。

## Final test verification

独立 worktree：`/tmp/v12-final-review`，HEAD 为 `718d2e1`。

- `go test ./...`: pass，exit code 0
- `go vet ./...`: pass，exit code 0
- `go test -race ./...`: pass，exit code 0
- `make test-e2e`: pass，exit code 0

已知 known-failing:

- None on reviewed HEAD. Earlier `tm pricing refresh` LiteLLM live schema blocker is fixed by `718d2e1`.

## P0/P1/P2 findings 分布

- P0: 0
- P1: 0
- P2: 2

P2 follow-ups:

- [P2] `tm --offline` / `tm --config path` without explicit subcommand still do not route to implicit daily. Workaround: use `tm daily --offline` or `tm daily --config path`.
- [P2] `tm analyze --project NAME` can merge same-basename checkouts. Workaround: use `--project-aliases` with explicit paths or `--all-projects` when cross-project view is intended.

## Self-review notes

- `bd28965`: help regression tests now cover daily instances/project aliases and analyze tool-errors/file-churn/project scope flags. Coverage is appropriate for help discoverability.
- `1dbd7df`: README quickstart no longer contains expected-exit-1 commands; unified config docs were later refreshed again after 892de23/4c415ea.
- `7b16df5` / `a0abdf1`: CHANGELOG commit references were cross-validated and corrected; no remaining ccusage-parity count inflation found.
- `6f2aa52`: advanced guides are dense enough for VitePress; project scope drift was corrected in 5b71714.
- `0bf7069`: marketing drafts avoid unsupported claims; license is MIT; “tested clean” was not claimed without evidence.
- `5b71714`: docs now consistently describe analyze insights as current-project by default, with `--all-projects` for whole-database behavior.

## Ship-day decision

✅ READY to tag from current HEAD, including `718d2e1`.

Do not tag a commit before `718d2e1`; without it, the LiteLLM JSON schema blocker remains. The two P2 findings are non-blocking and can be queued for v1.2.1 / v1.3.
