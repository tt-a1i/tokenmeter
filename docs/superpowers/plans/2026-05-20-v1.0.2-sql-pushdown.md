# v1.0.2 — SQL Push-Down 性能优化 实施 Plan

> **For Hive workers (star / cloud / 海盐):** 这是 v1.0.2 的执行清单。star 负责 storage/aggregate.go + benchmark；cloud 负责 cli 层切换 + blocks Breakdown + render；海盐做 Phase A/B/Ship 三 gate。每个 task 走 TDD：先写失败 test → 跑确认失败 → 写最小实现 → 跑确认通过 → `&&` chain atomic commit + 反向白名单 gate。

**Goal:** 把 daily/weekly/monthly/session 的 groupBy 从 cli 层内存聚合迁到 SQLite GROUP BY + SUM，warm <250ms（baseline 820ms / 4×）。

**Architecture:** 新增 `internal/storage/aggregate.go::AggregateUsage(filter)` 单一统一接口 → SQL 根据 `Bucket` 字段拼 `date(timestamp)` / `strftime('%Y-W%W', ...)` / `strftime('%Y-%m', ...)` / `session_id` 四种 group key。cli 层 RunAggregate / RunSession 改走新 API，删除 aggGroup 内存聚合。`blocks` 命令不动（5h 识别需要 raw timeline）。

**Tech Stack:** Go 1.24 · modernc.org/sqlite · 复用 v1.0.1 内置的 render / pricing 包 · `testing.B` benchmark · pprof

**Spec:** `docs/superpowers/specs/2026-05-20-v1.0.2-sql-pushdown-design.md`

---

## 文件结构（决策锁定）

新建：
- `internal/storage/aggregate.go` — `AggregateFilter` / `AggregateUsageRow` / `AggregateBucket` enum / `AggregateUsage` method
- `internal/storage/aggregate_test.go` — 单元测试覆盖 4 个 bucket + breakdown + timezone + project filter + empty range + GROUP_CONCAT 顺序
- `internal/storage/aggregate_bench_test.go` — benchmark（10k / 100k / 500k rows × Day/Week/Session）
- `docs/perf/v1.0.2-baseline.md` — v1.0.2 对比 v1.0.1 报告

修改：
- `internal/blocks/types.go` — `SessionBlock` 加 `PerModel map[string]TokenCounts` 字段
- `internal/blocks/burn.go` — Annotate 时填 PerModel
- `cmd/tm/cli/aggregate.go` — RunAggregate 切到 AggregateUsage；删 aggGroup / groupBy / bucketKey
- `cmd/tm/cli/session.go` — RunSession 切到 AggregateUsage（BucketSession）；删 sessionPathLoader（不再需要）
- `cmd/tm/cli/blocks.go` — Breakdown 路径填 BlockRow.Breakdown
- `cmd/tm/cli/aggregate_test.go` — 现有 test 用新 stub 跑通 + 加 push-down integration test
- `cmd/tm/cli/session_test.go` — 加 ProjectPath integration test
- `cmd/tm/cli/blocks_test.go` — 加 Breakdown 嵌套 test
- `internal/render/boxed.go` — RenderBlocks 加 `if opts.Breakdown { ... }` 嵌套分支
- `CHANGELOG.md` — v1.0.2 段
- `docs/MIGRATION-v1.0.md` — Mode=calculate 精度说明（如有）

---

## Phase A — storage 层 SQL push-down（Task 1-4）

### Task 1: `internal/storage/aggregate.go` types + AggregateUsage 骨架

**Owner:** star

**Files:**
- Create: `internal/storage/aggregate.go`

- [ ] **Step 1.1: 写 types 文件**

`internal/storage/aggregate.go`：

```go
// Package storage exposes AggregateUsage as the read-side counterpart to
// ListUsageForBlocksFiltered, but doing SUM / GROUP BY work in SQLite
// rather than pulling every row across the driver boundary.
package storage

import (
	"context"
	"fmt"
	"sort"
	"strings"
	"time"
)

// AggregateBucket selects the group key used by AggregateUsage.
type AggregateBucket int

const (
	BucketDay AggregateBucket = iota
	BucketWeek
	BucketMonth
	BucketSession
)

// AggregateFilter describes the read-side query AggregateUsage runs.
type AggregateFilter struct {
	Since     time.Time
	Until     time.Time
	Project   string         // sessions.cwd exact match; "" disables filter
	Bucket    AggregateBucket
	Breakdown bool           // true => add u.model as a secondary GROUP BY column
	Location  *time.Location // nil treated as UTC; affects day/week/month bucket key
}

// AggregateUsageRow is one row returned by AggregateUsage.
type AggregateUsageRow struct {
	Bucket            string    // "2026-05-19" / "2026-W21" / "2026-05" / session_id
	Model             string    // populated only when Breakdown=true
	Models            []string  // populated only when Breakdown=false (distinct, sorted)
	InputTokens       int64
	OutputTokens      int64
	CacheCreateTokens int64
	CacheReadTokens   int64
	Cost              float64
	Project           string    // populated only for BucketSession
	LastActivity      time.Time // populated only for BucketSession
}

// AggregateUsage runs a push-down SUM/GROUP BY query against token_usage
// joined to sessions. The returned rows are pre-sorted by Bucket ascending
// (and by Model ascending when Breakdown=true).
func (s *DB) AggregateUsage(ctx context.Context, f AggregateFilter) ([]AggregateUsageRow, error) {
	return nil, fmt.Errorf("AggregateUsage: not implemented yet (Task 2/3/4)")
}
```

- [ ] **Step 1.2: 编译通过**

```
go build ./internal/storage/
```

- [ ] **Step 1.3: `&&` chain atomic commit**

```bash
git add internal/storage/aggregate.go && \
  git diff --staged --stat && \
  git status --short && \
  [ -z "$(git diff --name-only --cached | grep -vE '^internal/storage/aggregate\.go$')" ] && \
  git commit -m "Add storage.AggregateUsage skeleton (types + stub)" && \
  git log -1 --stat
```

---

### Task 2: AggregateUsage 实现 BucketDay + Breakdown + Project filter

**Owner:** star

**Files:**
- Modify: `internal/storage/aggregate.go`
- Create: `internal/storage/aggregate_test.go`

- [ ] **Step 2.1: 写 8 个失败 test**

`internal/storage/aggregate_test.go`：

```go
package storage

import (
	"context"
	"testing"
	"time"
)

func mustTime(t *testing.T, s string) time.Time {
	ts, err := time.Parse(time.RFC3339, s)
	if err != nil { t.Fatalf("parse %q: %v", s, err) }
	return ts.UTC()
}

func openTestDB(t *testing.T) *DB {
	t.Helper()
	path := t.TempDir() + "/test.db"
	db, err := Open(path)
	if err != nil { t.Fatalf("open: %v", err) }
	t.Cleanup(func() { db.Close() })
	return db
}

func seedRow(t *testing.T, db *DB, sessionID, model string, ts time.Time, input, output, cacheCre, cacheRd int, cost float64) {
	t.Helper()
	if err := db.UpsertSession(sessionID, "claude", ts); err != nil {
		t.Fatalf("upsert session: %v", err)
	}
	if err := db.InsertTokenUsage("", sessionID, input, output, cacheCre, cacheRd, model, cost, ts, sessionID+"-"+ts.Format(time.RFC3339Nano)); err != nil {
		t.Fatalf("insert usage: %v", err)
	}
}

func TestAggregateUsageDay(t *testing.T) {
	db := openTestDB(t)
	seedRow(t, db, "s1", "claude", mustTime(t, "2026-05-19T10:00:00Z"), 100, 200, 10, 50, 1.0)
	seedRow(t, db, "s1", "claude", mustTime(t, "2026-05-19T11:00:00Z"), 50, 100, 5, 25, 0.5)
	seedRow(t, db, "s2", "gpt", mustTime(t, "2026-05-20T10:00:00Z"), 300, 400, 0, 0, 2.0)

	rows, err := db.AggregateUsage(context.Background(), AggregateFilter{Bucket: BucketDay})
	if err != nil { t.Fatalf("aggregate: %v", err) }
	if len(rows) != 2 { t.Fatalf("want 2 rows, got %d", len(rows)) }
	if rows[0].Bucket != "2026-05-19" { t.Errorf("row 0 bucket: %q", rows[0].Bucket) }
	if rows[0].InputTokens != 150 { t.Errorf("row 0 input: %d", rows[0].InputTokens) }
	if rows[0].OutputTokens != 300 { t.Errorf("row 0 output: %d", rows[0].OutputTokens) }
	if rows[0].Cost != 1.5 { t.Errorf("row 0 cost: %v", rows[0].Cost) }
	if len(rows[0].Models) != 1 || rows[0].Models[0] != "claude" {
		t.Errorf("row 0 models: %v", rows[0].Models)
	}
}

func TestAggregateUsageDayBreakdown(t *testing.T) {
	db := openTestDB(t)
	seedRow(t, db, "s1", "claude", mustTime(t, "2026-05-19T10:00:00Z"), 100, 200, 0, 0, 1.0)
	seedRow(t, db, "s1", "gpt", mustTime(t, "2026-05-19T11:00:00Z"), 50, 100, 0, 0, 0.5)

	rows, err := db.AggregateUsage(context.Background(), AggregateFilter{
		Bucket: BucketDay, Breakdown: true,
	})
	if err != nil { t.Fatalf("aggregate: %v", err) }
	if len(rows) != 2 { t.Fatalf("want 2 breakdown rows, got %d", len(rows)) }
	if rows[0].Bucket != "2026-05-19" || rows[0].Model != "claude" {
		t.Errorf("row 0 want claude, got %+v", rows[0])
	}
	if rows[1].Model != "gpt" {
		t.Errorf("row 1 want gpt, got %+v", rows[1])
	}
}

func TestAggregateUsageProjectFilter(t *testing.T) {
	db := openTestDB(t)
	seedRow(t, db, "s1", "claude", mustTime(t, "2026-05-19T10:00:00Z"), 100, 0, 0, 0, 1.0)
	seedRow(t, db, "s2", "claude", mustTime(t, "2026-05-19T11:00:00Z"), 200, 0, 0, 0, 2.0)
	// give s1 a cwd; s2 doesn't get one
	if err := db.UpdateSessionMeta("s1", "/code/foo", "main"); err != nil {
		t.Fatalf("update meta: %v", err)
	}

	rows, err := db.AggregateUsage(context.Background(), AggregateFilter{
		Bucket: BucketDay, Project: "/code/foo",
	})
	if err != nil { t.Fatalf("aggregate: %v", err) }
	if len(rows) != 1 { t.Fatalf("want 1 row, got %d", len(rows)) }
	if rows[0].InputTokens != 100 {
		t.Errorf("expected only s1 (input=100), got %d", rows[0].InputTokens)
	}
}

func TestAggregateUsageDateRange(t *testing.T) {
	db := openTestDB(t)
	seedRow(t, db, "s1", "x", mustTime(t, "2026-05-18T10:00:00Z"), 10, 0, 0, 0, 0)
	seedRow(t, db, "s2", "x", mustTime(t, "2026-05-19T10:00:00Z"), 20, 0, 0, 0, 0)
	seedRow(t, db, "s3", "x", mustTime(t, "2026-05-20T10:00:00Z"), 30, 0, 0, 0, 0)

	rows, err := db.AggregateUsage(context.Background(), AggregateFilter{
		Bucket: BucketDay,
		Since:  mustTime(t, "2026-05-19T00:00:00Z"),
		Until:  mustTime(t, "2026-05-20T00:00:00Z"),
	})
	if err != nil { t.Fatalf("aggregate: %v", err) }
	if len(rows) != 1 || rows[0].Bucket != "2026-05-19" {
		t.Fatalf("expected only 5-19, got %+v", rows)
	}
}

func TestAggregateUsageEmptyRange(t *testing.T) {
	db := openTestDB(t)
	rows, err := db.AggregateUsage(context.Background(), AggregateFilter{Bucket: BucketDay})
	if err != nil { t.Fatalf("aggregate: %v", err) }
	if len(rows) != 0 { t.Fatalf("expected no rows, got %d", len(rows)) }
}

func TestAggregateUsageGroupConcatModelOrder(t *testing.T) {
	// SQLite GROUP_CONCAT(DISTINCT) order is unspecified; AggregateUsage
	// must sort Models alphabetically before returning.
	db := openTestDB(t)
	seedRow(t, db, "s1", "zzz", mustTime(t, "2026-05-19T10:00:00Z"), 1, 0, 0, 0, 0)
	seedRow(t, db, "s1", "aaa", mustTime(t, "2026-05-19T11:00:00Z"), 1, 0, 0, 0, 0)
	seedRow(t, db, "s1", "mmm", mustTime(t, "2026-05-19T12:00:00Z"), 1, 0, 0, 0, 0)

	rows, err := db.AggregateUsage(context.Background(), AggregateFilter{Bucket: BucketDay})
	if err != nil { t.Fatalf("aggregate: %v", err) }
	if len(rows) != 1 { t.Fatalf("want 1 row, got %d", len(rows)) }
	got := rows[0].Models
	want := []string{"aaa", "mmm", "zzz"}
	for i := range want {
		if i >= len(got) || got[i] != want[i] {
			t.Errorf("models[%d]: got %q want %q (full: %v)", i, got[i], want[i], got)
		}
	}
}
```

- [ ] **Step 2.2: 跑测试确认全 fail**

```
go test ./internal/storage/ -run TestAggregateUsage -v
```
预期：全 fail with "AggregateUsage: not implemented yet"

- [ ] **Step 2.3: 实现 BucketDay + Breakdown + Project filter**

替换 `internal/storage/aggregate.go::AggregateUsage` stub：

```go
func (s *DB) AggregateUsage(ctx context.Context, f AggregateFilter) ([]AggregateUsageRow, error) {
	bucketExpr, err := s.bucketExpr(f.Bucket, f.Location)
	if err != nil { return nil, err }

	var (
		modelCol  string
		groupCols string
		orderCols string
	)
	if f.Breakdown {
		modelCol = "u.model"
		groupCols = bucketExpr + ", u.model"
		orderCols = "1 ASC, u.model ASC"
	} else {
		modelCol = "GROUP_CONCAT(DISTINCT u.model)"
		groupCols = bucketExpr
		orderCols = "1 ASC"
	}

	q := `SELECT ` + bucketExpr + ` AS bucket,
	             ` + modelCol + ` AS model_col,
	             COALESCE(SUM(u.input_tokens), 0),
	             COALESCE(SUM(u.output_tokens), 0),
	             COALESCE(SUM(u.cache_creation_tokens), 0),
	             COALESCE(SUM(u.cache_read_tokens), 0),
	             COALESCE(SUM(u.cost_usd), 0)`
	if f.Bucket == BucketSession {
		q += `, MAX(s.cwd) AS project,
		         MAX(u.timestamp) AS last_activity`
	}
	q += `
	FROM token_usage u
	JOIN sessions s ON s.session_id = u.session_id`

	var args []any
	var wheres []string
	if !f.Since.IsZero() {
		wheres = append(wheres, "u.timestamp >= ?")
		args = append(args, formatStorageTime(f.Since))
	}
	if !f.Until.IsZero() {
		wheres = append(wheres, "u.timestamp <= ?")
		args = append(args, formatStorageTime(f.Until))
	}
	if f.Project != "" {
		wheres = append(wheres, "s.cwd = ?")
		args = append(args, f.Project)
	}
	if len(wheres) > 0 {
		q += " WHERE " + strings.Join(wheres, " AND ")
	}
	q += " GROUP BY " + groupCols + " ORDER BY " + orderCols

	rows, err := s.db.QueryContext(ctx, q, args...)
	if err != nil { return nil, fmt.Errorf("aggregate usage: %w", err) }
	defer rows.Close()

	var out []AggregateUsageRow
	for rows.Next() {
		var (
			r           AggregateUsageRow
			modelStr    sql.NullString
			project     sql.NullString
			lastActStr  sql.NullString
		)
		dest := []any{
			&r.Bucket, &modelStr,
			&r.InputTokens, &r.OutputTokens,
			&r.CacheCreateTokens, &r.CacheReadTokens, &r.Cost,
		}
		if f.Bucket == BucketSession {
			dest = append(dest, &project, &lastActStr)
		}
		if err := rows.Scan(dest...); err != nil {
			return nil, fmt.Errorf("scan aggregate row: %w", err)
		}
		if f.Breakdown {
			r.Model = modelStr.String
		} else {
			r.Models = splitAndSortModels(modelStr.String)
		}
		if f.Bucket == BucketSession {
			r.Project = project.String
			if lastActStr.Valid {
				if ts, ok := parseStorageTime(lastActStr.String); ok {
					r.LastActivity = ts
				}
			}
		}
		out = append(out, r)
	}
	return out, rows.Err()
}

func (s *DB) bucketExpr(bucket AggregateBucket, loc *time.Location) (string, error) {
	tzMod := ""
	if loc != nil && loc != time.UTC {
		_, offset := time.Now().In(loc).Zone()
		// SQLite date modifiers take "+N hours" etc. We pass seconds for precision.
		tzMod = fmt.Sprintf(", '%+d seconds'", offset)
	}
	switch bucket {
	case BucketDay:
		return "date(u.timestamp" + tzMod + ")", nil
	case BucketWeek:
		return "strftime('%Y-W%W', u.timestamp" + tzMod + ")", nil
	case BucketMonth:
		return "strftime('%Y-%m', u.timestamp" + tzMod + ")", nil
	case BucketSession:
		return "u.session_id", nil
	default:
		return "", fmt.Errorf("unknown AggregateBucket %d", bucket)
	}
}

func splitAndSortModels(joined string) []string {
	if joined == "" { return nil }
	parts := strings.Split(joined, ",")
	out := parts[:0]
	for _, p := range parts {
		p = strings.TrimSpace(p)
		if p != "" { out = append(out, p) }
	}
	sort.Strings(out)
	return out
}
```

确保文件顶部 import 包含 `"database/sql"`、`"context"`、`"fmt"`、`"sort"`、`"strings"`、`"time"`。

- [ ] **Step 2.4: 跑测试确认通过**

```
go test ./internal/storage/ -run TestAggregateUsage -v
```

预期：6 个 test 全过（Day, DayBreakdown, ProjectFilter, DateRange, EmptyRange, GroupConcatModelOrder）

- [ ] **Step 2.5: lint**

```
golangci-lint run ./internal/storage/...
go vet ./internal/storage/
```

- [ ] **Step 2.6: `&&` chain atomic commit + 反向白名单 gate**

```bash
git add internal/storage/aggregate.go internal/storage/aggregate_test.go && \
  git diff --staged --stat && \
  git status --short && \
  [ -z "$(git diff --name-only --cached | grep -vE '^internal/storage/aggregate(\.go|_test\.go)$')" ] && \
  git commit -m "Implement AggregateUsage Day bucket + Breakdown + Project filter" && \
  git log -1 --stat
```

---

### Task 3: AggregateUsage 加 BucketWeek / BucketMonth + Timezone

**Owner:** star

**Files:**
- Modify: `internal/storage/aggregate_test.go`（加测试）

bucketExpr 已经覆盖 Week / Month，本 task 只补测试 + 一个 Timezone test。

- [ ] **Step 3.1: 加 3 个 test**

`internal/storage/aggregate_test.go` 加：

```go
func TestAggregateUsageWeek(t *testing.T) {
	db := openTestDB(t)
	// 2026-05-19 is Tuesday, week 20 (per strftime %W which uses
	// Monday-as-first-day-of-week, same as Go ISO week here).
	seedRow(t, db, "s1", "x", mustTime(t, "2026-05-19T10:00:00Z"), 10, 0, 0, 0, 0)
	seedRow(t, db, "s2", "x", mustTime(t, "2026-05-25T10:00:00Z"), 20, 0, 0, 0, 0)
	// 2026-05-25 is Monday week 21

	rows, err := db.AggregateUsage(context.Background(), AggregateFilter{Bucket: BucketWeek})
	if err != nil { t.Fatalf("aggregate: %v", err) }
	if len(rows) != 2 { t.Fatalf("want 2 weeks, got %d (%+v)", len(rows), rows) }
	if rows[0].Bucket != "2026-W20" || rows[1].Bucket != "2026-W21" {
		t.Errorf("buckets: %q / %q", rows[0].Bucket, rows[1].Bucket)
	}
}

func TestAggregateUsageMonth(t *testing.T) {
	db := openTestDB(t)
	seedRow(t, db, "s1", "x", mustTime(t, "2026-04-15T10:00:00Z"), 10, 0, 0, 0, 0)
	seedRow(t, db, "s2", "x", mustTime(t, "2026-05-15T10:00:00Z"), 20, 0, 0, 0, 0)

	rows, err := db.AggregateUsage(context.Background(), AggregateFilter{Bucket: BucketMonth})
	if err != nil { t.Fatalf("aggregate: %v", err) }
	if len(rows) != 2 { t.Fatalf("want 2 months, got %d", len(rows)) }
	if rows[0].Bucket != "2026-04" || rows[1].Bucket != "2026-05" {
		t.Errorf("months: %q / %q", rows[0].Bucket, rows[1].Bucket)
	}
}

func TestAggregateUsageTimezoneOffset(t *testing.T) {
	db := openTestDB(t)
	// 2026-05-19 23:30 UTC == 2026-05-20 07:30 Asia/Shanghai (+8h)
	seedRow(t, db, "s1", "x", mustTime(t, "2026-05-19T23:30:00Z"), 100, 0, 0, 0, 0)

	shanghai, err := time.LoadLocation("Asia/Shanghai")
	if err != nil { t.Skip("Asia/Shanghai tz data missing") }

	rows, err := db.AggregateUsage(context.Background(), AggregateFilter{
		Bucket: BucketDay, Location: shanghai,
	})
	if err != nil { t.Fatalf("aggregate: %v", err) }
	if len(rows) != 1 || rows[0].Bucket != "2026-05-20" {
		t.Fatalf("want 2026-05-20 bucket (Shanghai TZ), got %+v", rows)
	}
}
```

- [ ] **Step 3.2: 跑测试确认通过**

```
go test ./internal/storage/ -run TestAggregateUsage -v
```

预期：9 个 test 全过（前 6 个 + Week / Month / TimezoneOffset）

- [ ] **Step 3.3: `&&` chain atomic commit**

```bash
git add internal/storage/aggregate_test.go && \
  git diff --staged --stat && \
  git status --short && \
  [ -z "$(git diff --name-only --cached | grep -vE '^internal/storage/aggregate_test\.go$')" ] && \
  git commit -m "Cover Week, Month and Timezone bucket paths in AggregateUsage" && \
  git log -1 --stat
```

---

### Task 4: BucketSession + LastActivity + Project

**Owner:** star

**Files:**
- Modify: `internal/storage/aggregate_test.go`（加测试）

bucketExpr 已经覆盖 Session；query 已经在 BucketSession 时多 SELECT cwd + MAX(timestamp)。本 task 只补测试。

- [ ] **Step 4.1: 加 2 个 session test**

```go
func TestAggregateUsageSession(t *testing.T) {
	db := openTestDB(t)
	seedRow(t, db, "s1", "claude", mustTime(t, "2026-05-19T10:00:00Z"), 100, 200, 0, 0, 1.0)
	seedRow(t, db, "s1", "claude", mustTime(t, "2026-05-19T11:00:00Z"), 50, 100, 0, 0, 0.5)
	seedRow(t, db, "s2", "gpt", mustTime(t, "2026-05-20T10:00:00Z"), 300, 0, 0, 0, 2.0)
	if err := db.UpdateSessionMeta("s1", "/code/foo", "main"); err != nil {
		t.Fatalf("update meta: %v", err)
	}

	rows, err := db.AggregateUsage(context.Background(), AggregateFilter{Bucket: BucketSession})
	if err != nil { t.Fatalf("aggregate: %v", err) }
	if len(rows) != 2 { t.Fatalf("want 2 sessions, got %d", len(rows)) }
	// rows ordered by session_id ASC ("s1", "s2")
	if rows[0].Bucket != "s1" { t.Errorf("row 0 bucket: %q", rows[0].Bucket) }
	if rows[0].Project != "/code/foo" { t.Errorf("row 0 project: %q", rows[0].Project) }
	if rows[0].InputTokens != 150 { t.Errorf("row 0 input: %d", rows[0].InputTokens) }
	expectedLast := mustTime(t, "2026-05-19T11:00:00Z")
	if !rows[0].LastActivity.Equal(expectedLast) {
		t.Errorf("row 0 last_activity: got %v, want %v", rows[0].LastActivity, expectedLast)
	}
	if rows[1].Bucket != "s2" { t.Errorf("row 1 bucket: %q", rows[1].Bucket) }
	if rows[1].Project != "" { t.Errorf("row 1 project should be empty: %q", rows[1].Project) }
}

func TestAggregateUsageSessionBreakdown(t *testing.T) {
	db := openTestDB(t)
	seedRow(t, db, "s1", "claude", mustTime(t, "2026-05-19T10:00:00Z"), 100, 0, 0, 0, 1.0)
	seedRow(t, db, "s1", "gpt", mustTime(t, "2026-05-19T11:00:00Z"), 50, 0, 0, 0, 0.5)

	rows, err := db.AggregateUsage(context.Background(), AggregateFilter{
		Bucket: BucketSession, Breakdown: true,
	})
	if err != nil { t.Fatalf("aggregate: %v", err) }
	if len(rows) != 2 { t.Fatalf("want 2 session+model rows, got %d", len(rows)) }
	if rows[0].Bucket != "s1" || rows[0].Model != "claude" {
		t.Errorf("row 0: %+v", rows[0])
	}
	if rows[1].Model != "gpt" {
		t.Errorf("row 1: %+v", rows[1])
	}
}
```

- [ ] **Step 4.2: 跑测试确认通过**

```
go test ./internal/storage/ -run TestAggregateUsage -v
```

预期：11 个 test 全过

- [ ] **Step 4.3: `&&` chain atomic commit**

```bash
git add internal/storage/aggregate_test.go && \
  git diff --staged --stat && \
  git status --short && \
  [ -z "$(git diff --name-only --cached | grep -vE '^internal/storage/aggregate_test\.go$')" ] && \
  git commit -m "Cover Session bucket with project + last_activity in AggregateUsage" && \
  git log -1 --stat
```

---

**Phase A 边界**：海盐 gate Tasks 1-4 完整后再启动 Phase B。

---

## Phase B — cli 层切换（Task 5-6）

### Task 5: `cli/aggregate.go` 切到 AggregateUsage

**Owner:** cloud

**Files:**
- Modify: `cmd/tm/cli/aggregate.go`
- Modify: `cmd/tm/cli/aggregate_test.go`

- [ ] **Step 5.1: 扩 AggregateLoader interface**

`cmd/tm/cli/aggregate.go` 顶部找到 `type AggregateLoader interface { ... }`，加一行：

```go
type AggregateLoader interface {
	ListUsageForBlocksFiltered(ctx context.Context, since, until time.Time, workspace string) ([]storage.TokenUsageEntry, error)
	AggregateUsage(ctx context.Context, f storage.AggregateFilter) ([]storage.AggregateUsageRow, error)  // new
}
```

`*storage.DB` 已经实现两个方法。每个 test stub 加一个 `AggregateUsage` 方法（返回 fixture）。

- [ ] **Step 5.2: 写失败 test — push-down integration**

在 `cmd/tm/cli/aggregate_test.go` 加：

```go
// stubAggregateLoaderPushdown returns fixed AggregateUsageRow set, used to
// verify RunAggregate now consumes AggregateUsage rather than re-aggregating
// raw entries in CLI.
type stubAggregateLoaderPushdown struct {
	rows []storage.AggregateUsageRow
}

func (s stubAggregateLoaderPushdown) ListUsageForBlocksFiltered(ctx context.Context, since, until time.Time, workspace string) ([]storage.TokenUsageEntry, error) {
	return nil, nil // unused by RunAggregate post-Task 5
}
func (s stubAggregateLoaderPushdown) AggregateUsage(ctx context.Context, f storage.AggregateFilter) ([]storage.AggregateUsageRow, error) {
	return s.rows, nil
}

func TestRunAggregateUsesPushdownAPI(t *testing.T) {
	rows := []storage.AggregateUsageRow{
		{Bucket: "2026-05-19", Models: []string{"claude"}, InputTokens: 100, Cost: 1.0},
		{Bucket: "2026-05-20", Models: []string{"claude"}, InputTokens: 200, Cost: 2.0},
	}
	args := cli.AggregateArgs{
		Shared: cli.Shared{JSON: true},
		Bucket: cli.BucketDaily,
	}
	var buf bytes.Buffer
	if err := cli.RunAggregate(context.Background(), &buf, args, stubAggregateLoaderPushdown{rows: rows}); err != nil {
		t.Fatal(err)
	}
	out := buf.String()
	if !strings.Contains(out, `"date": "2026-05-19"`) {
		t.Errorf("expected 2026-05-19 in output:\n%s", out)
	}
	if !strings.Contains(out, `"inputTokens": 100`) {
		t.Errorf("expected inputTokens=100:\n%s", out)
	}
}
```

- [ ] **Step 5.3: 跑测试确认 fail**

```
go test ./cmd/tm/cli/ -run TestRunAggregateUsesPushdownAPI -v
```

预期：fail（接口未扩 / RunAggregate 仍走 ListUsageForBlocksFiltered）

- [ ] **Step 5.4: 重写 RunAggregate**

`cmd/tm/cli/aggregate.go` 替换 RunAggregate（及 groupBy / aggGroup / modelStats 全删）：

```go
package cli

import (
	"context"
	"fmt"
	"io"
	"sort"
	"time"

	"github.com/tt-a1i/tokenmeter/internal/pricing"
	"github.com/tt-a1i/tokenmeter/internal/render"
	"github.com/tt-a1i/tokenmeter/internal/storage"
)

type Bucket int

const (
	BucketDaily Bucket = iota
	BucketWeekly
	BucketMonthly
)

type AggregateArgs struct {
	Shared Shared
	Bucket Bucket
}

type AggregateLoader interface {
	ListUsageForBlocksFiltered(ctx context.Context, since, until time.Time, workspace string) ([]storage.TokenUsageEntry, error)
	AggregateUsage(ctx context.Context, f storage.AggregateFilter) ([]storage.AggregateUsageRow, error)
}

func RunAggregate(ctx context.Context, w io.Writer, a AggregateArgs, loader AggregateLoader) error {
	since, err := parseDateFlag(a.Shared.Since)
	if err != nil { return err }
	until, err := ParseDateFlagUntil(a.Shared.Until)
	if err != nil { return err }

	loc := time.UTC
	if a.Shared.Timezone != "" {
		parsed, err := time.LoadLocation(a.Shared.Timezone)
		if err != nil { return fmt.Errorf("invalid timezone %q: %w", a.Shared.Timezone, err) }
		loc = parsed
	}

	filter := storage.AggregateFilter{
		Since:     since,
		Until:     until,
		Project:   a.Shared.Project,
		Bucket:    cliBucketToStorage(a.Bucket),
		Breakdown: a.Shared.Breakdown,
		Location:  loc,
	}
	aggRows, err := loader.AggregateUsage(ctx, filter)
	if err != nil { return err }

	mode := pricing.ParseMode(a.Shared.Mode)
	rows := convertAggregateRows(aggRows, a.Shared.Breakdown, mode)

	if a.Shared.Order == "desc" {
		sort.Slice(rows, func(i, j int) bool { return rows[i].Bucket > rows[j].Bucket })
	}

	return render.New().RenderAggregate(w, bucketKind(a.Bucket), rows, renderOpts(a.Shared, w))
}

func cliBucketToStorage(b Bucket) storage.AggregateBucket {
	switch b {
	case BucketWeekly: return storage.BucketWeek
	case BucketMonthly: return storage.BucketMonth
	default: return storage.BucketDay
	}
}

func bucketKind(b Bucket) string {
	switch b {
	case BucketWeekly: return "weekly"
	case BucketMonthly: return "monthly"
	default: return "daily"
	}
}

// convertAggregateRows folds storage rows (which may have one row per
// (bucket, model) when Breakdown=true) into render rows (one row per
// bucket, with Breakdown[] populated when requested).
func convertAggregateRows(in []storage.AggregateUsageRow, breakdown bool, mode pricing.Mode) []render.AggregateRow {
	if !breakdown {
		out := make([]render.AggregateRow, 0, len(in))
		for _, r := range in {
			cost := r.Cost
			if mode == pricing.ModeCalculate {
				cost = recalculateCost(r, mode)
			}
			out = append(out, render.AggregateRow{
				Bucket:            r.Bucket,
				Models:            r.Models,
				InputTokens:       r.InputTokens,
				OutputTokens:      r.OutputTokens,
				CacheCreateTokens: r.CacheCreateTokens,
				CacheReadTokens:   r.CacheReadTokens,
				TotalTokens:       r.InputTokens + r.OutputTokens + r.CacheCreateTokens + r.CacheReadTokens,
				Cost:              cost,
			})
		}
		return out
	}
	// Breakdown=true: fold by bucket
	byBucket := map[string]*render.AggregateRow{}
	order := []string{}
	for _, r := range in {
		row, ok := byBucket[r.Bucket]
		if !ok {
			row = &render.AggregateRow{Bucket: r.Bucket}
			byBucket[r.Bucket] = row
			order = append(order, r.Bucket)
		}
		cost := r.Cost
		if mode == pricing.ModeCalculate {
			cost = recalculateCost(r, mode)
		}
		row.Models = appendUnique(row.Models, r.Model)
		row.InputTokens += r.InputTokens
		row.OutputTokens += r.OutputTokens
		row.CacheCreateTokens += r.CacheCreateTokens
		row.CacheReadTokens += r.CacheReadTokens
		row.TotalTokens = row.InputTokens + row.OutputTokens + row.CacheCreateTokens + row.CacheReadTokens
		row.Cost += cost
		row.Breakdown = append(row.Breakdown, render.ModelBreakdown{
			Model:             r.Model,
			InputTokens:       r.InputTokens,
			OutputTokens:      r.OutputTokens,
			CacheCreateTokens: r.CacheCreateTokens,
			CacheReadTokens:   r.CacheReadTokens,
			TotalTokens:       r.InputTokens + r.OutputTokens + r.CacheCreateTokens + r.CacheReadTokens,
			Cost:              cost,
		})
	}
	out := make([]render.AggregateRow, 0, len(order))
	for _, k := range order {
		out = append(out, *byBucket[k])
	}
	return out
}

func appendUnique(slice []string, v string) []string {
	for _, s := range slice { if s == v { return slice } }
	return append(slice, v)
}

// recalculateCost is the SUM-then-reprice path for Mode=calculate. SQL gave
// us bucketed sums; we re-price using the bucket's primary model (Models[0]
// or r.Model when Breakdown). For mixed-model buckets in non-breakdown mode
// this is an approximation; users wanting exact per-entry recompute should
// use Breakdown=true.
func recalculateCost(r storage.AggregateUsageRow, _ pricing.Mode) float64 {
	model := r.Model
	if model == "" && len(r.Models) > 0 { model = r.Models[0] }
	if model == "" { return r.Cost }
	p, ok := pricing.Resolve(pricingMap, model)
	if !ok { return r.Cost }
	return pricing.CalculateCost(p, pricing.Usage{
		Input:       r.InputTokens,
		Output:      r.OutputTokens,
		CacheCreate: r.CacheCreateTokens,
		CacheRead:   r.CacheReadTokens,
	}, pricing.SpeedStandard)
}
```

确保 `pricingMap` package var 仍在（v1.0.1 Task 1 加的）。如果之前在 aggregate.go 里，保留。

- [ ] **Step 5.5: 跑测试确认通过**

```
go test ./cmd/tm/cli/ -v
golangci-lint run ./cmd/tm/cli/...
```

预期：全 PASS（含新 TestRunAggregateUsesPushdownAPI；旧 Order/Mode/Timezone/Breakdown test 不能破，stub 需要实现 AggregateUsage 方法返预期 fixture — 见 Step 5.6）

- [ ] **Step 5.6: 更新旧 stub 测试**

旧的 `stubAggregateLoader{rows: []TokenUsageEntry{...}}` 不再被 RunAggregate 用。两种处理：

(a) **推荐**：把所有 RunAggregate 旧 test 改成用 `stubAggregateLoaderPushdown{rows: []AggregateUsageRow{...}}`，直接喂聚合 row。这是 cli 层真实新契约。

(b) 在 `stubAggregateLoader` 上加一个 helper `func (s stubAggregateLoader) AggregateUsage(ctx, f)` 内部对 entries 做 in-process aggregation（fake）。

走 (a)：把 TestRunAggregateOrderDesc / TimezoneBucketing / ModeCalculate / BreakdownPropagates 4 个 test 的 stub 改成 stubAggregateLoaderPushdown，喂直接的聚合 fixture。Timezone test 改成验 cliBucketToStorage 把 Location 传下去（用一个 wrapper stub 捕获 filter.Location）：

```go
type capturingLoader struct {
	captured storage.AggregateFilter
	rows     []storage.AggregateUsageRow
}

func (c *capturingLoader) ListUsageForBlocksFiltered(ctx context.Context, since, until time.Time, workspace string) ([]storage.TokenUsageEntry, error) {
	return nil, nil
}
func (c *capturingLoader) AggregateUsage(ctx context.Context, f storage.AggregateFilter) ([]storage.AggregateUsageRow, error) {
	c.captured = f
	return c.rows, nil
}

func TestRunAggregateTimezonePropagation(t *testing.T) {
	stub := &capturingLoader{rows: nil}
	args := cli.AggregateArgs{
		Shared: cli.Shared{Timezone: "Asia/Shanghai"},
		Bucket: cli.BucketDaily,
	}
	if err := cli.RunAggregate(context.Background(), io.Discard, args, stub); err != nil {
		t.Fatal(err)
	}
	if stub.captured.Location == nil || stub.captured.Location.String() != "Asia/Shanghai" {
		t.Errorf("expected Location=Asia/Shanghai, got %v", stub.captured.Location)
	}
}
```

旧 TimezoneBucketing test 改成 propagation 测试（实际 bucketing 由 storage 测试覆盖，cli 只验参数传递）。

Order=desc test 直接喂 2 个 bucket，验输出顺序。Mode=calculate test 喂含 InputTokens 的聚合 row + 零 cost，验 cli 端 recalculateCost 重算 cost。

- [ ] **Step 5.7: 跑全包测试**

```
go test ./cmd/tm/cli/ -v -count=1
```

预期：全 PASS

- [ ] **Step 5.8: `&&` chain atomic commit + 反向白名单 gate**

```bash
git add cmd/tm/cli/aggregate.go cmd/tm/cli/aggregate_test.go && \
  git diff --staged --stat && \
  git status --short && \
  [ -z "$(git diff --name-only --cached | grep -vE '^cmd/tm/cli/aggregate(\.go|_test\.go)$')" ] && \
  git commit -m "Switch RunAggregate to storage.AggregateUsage push-down API" && \
  git log -1 --stat
```

---

### Task 6: `cli/session.go` 切到 AggregateUsage

**Owner:** cloud

**Files:**
- Modify: `cmd/tm/cli/session.go`
- Modify: `cmd/tm/cli/session_test.go`

- [ ] **Step 6.1: 写失败 test — ProjectPath in JSON**

`cmd/tm/cli/session_test.go` 加：

```go
func TestRunSessionProjectPathInJSON(t *testing.T) {
	rows := []storage.AggregateUsageRow{
		{
			Bucket: "abc-123", Models: []string{"claude"},
			InputTokens: 100, Cost: 1.0,
			Project:      "/code/foo",
			LastActivity: mustParseTimeT(t, "2026-05-19T11:30:00Z"),
		},
	}
	args := cli.SessionArgs{Shared: cli.Shared{JSON: true}}
	var buf bytes.Buffer
	if err := cli.RunSession(context.Background(), &buf, args, stubAggregateLoaderPushdown{rows: rows}); err != nil {
		t.Fatal(err)
	}
	out := buf.String()
	if !strings.Contains(out, `"projectPath": "/code/foo"`) {
		t.Errorf("expected projectPath in output:\n%s", out)
	}
	if !strings.Contains(out, `"sessionId": "abc-123"`) {
		t.Errorf("expected sessionId in output:\n%s", out)
	}
}
```

- [ ] **Step 6.2: 跑测试确认 fail**

```
go test ./cmd/tm/cli/ -run TestRunSessionProjectPath -v
```

预期：fail（RunSession 仍走旧 path）

- [ ] **Step 6.3: 重写 RunSession**

`cmd/tm/cli/session.go` 全文替换：

```go
package cli

import (
	"context"
	"fmt"
	"io"
	"sort"
	"time"

	"github.com/tt-a1i/tokenmeter/internal/pricing"
	"github.com/tt-a1i/tokenmeter/internal/render"
	"github.com/tt-a1i/tokenmeter/internal/storage"
)

type SessionArgs struct {
	Shared    Shared
	SessionID string // optional; "" lists all
}

func RunSession(ctx context.Context, w io.Writer, a SessionArgs, loader AggregateLoader) error {
	since, err := parseDateFlag(a.Shared.Since)
	if err != nil { return err }
	until, err := ParseDateFlagUntil(a.Shared.Until)
	if err != nil { return err }

	loc := time.UTC
	if a.Shared.Timezone != "" {
		parsed, err := time.LoadLocation(a.Shared.Timezone)
		if err != nil { return fmt.Errorf("invalid timezone %q: %w", a.Shared.Timezone, err) }
		loc = parsed
	}

	filter := storage.AggregateFilter{
		Since:     since,
		Until:     until,
		Project:   a.Shared.Project,
		Bucket:    storage.BucketSession,
		Breakdown: a.Shared.Breakdown,
		Location:  loc,
	}
	aggRows, err := loader.AggregateUsage(ctx, filter)
	if err != nil { return err }

	mode := pricing.ParseMode(a.Shared.Mode)
	rows := convertSessionRows(aggRows, a.Shared.Breakdown, mode, a.SessionID)

	if a.Shared.Order == "desc" {
		sort.Slice(rows, func(i, j int) bool { return rows[i].SessionID > rows[j].SessionID })
	}

	return render.New().RenderSessions(w, rows, renderOpts(a.Shared, w))
}

func convertSessionRows(in []storage.AggregateUsageRow, breakdown bool, mode pricing.Mode, filterID string) []render.SessionRow {
	if !breakdown {
		out := make([]render.SessionRow, 0, len(in))
		for _, r := range in {
			if filterID != "" && r.Bucket != filterID { continue }
			cost := r.Cost
			if mode == pricing.ModeCalculate {
				cost = recalculateCost(r, mode)
			}
			out = append(out, render.SessionRow{
				SessionID:         r.Bucket,
				ProjectPath:       r.Project,
				LastActivity:      r.LastActivity,
				Models:            r.Models,
				InputTokens:       r.InputTokens,
				OutputTokens:      r.OutputTokens,
				CacheCreateTokens: r.CacheCreateTokens,
				CacheReadTokens:   r.CacheReadTokens,
				TotalTokens:       r.InputTokens + r.OutputTokens + r.CacheCreateTokens + r.CacheReadTokens,
				Cost:              cost,
			})
		}
		return out
	}
	// Breakdown=true: fold by session
	bySession := map[string]*render.SessionRow{}
	order := []string{}
	for _, r := range in {
		if filterID != "" && r.Bucket != filterID { continue }
		row, ok := bySession[r.Bucket]
		if !ok {
			row = &render.SessionRow{
				SessionID:    r.Bucket,
				ProjectPath:  r.Project,
				LastActivity: r.LastActivity,
			}
			bySession[r.Bucket] = row
			order = append(order, r.Bucket)
		}
		cost := r.Cost
		if mode == pricing.ModeCalculate {
			cost = recalculateCost(r, mode)
		}
		row.Models = appendUnique(row.Models, r.Model)
		row.InputTokens += r.InputTokens
		row.OutputTokens += r.OutputTokens
		row.CacheCreateTokens += r.CacheCreateTokens
		row.CacheReadTokens += r.CacheReadTokens
		row.TotalTokens = row.InputTokens + row.OutputTokens + row.CacheCreateTokens + row.CacheReadTokens
		row.Cost += cost
		if r.LastActivity.After(row.LastActivity) {
			row.LastActivity = r.LastActivity
		}
		row.Breakdown = append(row.Breakdown, render.ModelBreakdown{
			Model:             r.Model,
			InputTokens:       r.InputTokens,
			OutputTokens:      r.OutputTokens,
			CacheCreateTokens: r.CacheCreateTokens,
			CacheReadTokens:   r.CacheReadTokens,
			TotalTokens:       r.InputTokens + r.OutputTokens + r.CacheCreateTokens + r.CacheReadTokens,
			Cost:              cost,
		})
	}
	out := make([]render.SessionRow, 0, len(order))
	for _, k := range order {
		out = append(out, *bySession[k])
	}
	return out
}
```

删除：旧 `sessionPathLoader` interface、`tabwriter` 残留（如果有）、内存聚合 loop。

- [ ] **Step 6.4: 更新旧 session test stub**

旧 TestRunSessionListMode/OrderDesc/ModeCalculate 4 个 test 改成喂 `stubAggregateLoaderPushdown` + 聚合 fixture。Mode=calculate 验 cli 端 recalculate 走通。

- [ ] **Step 6.5: 跑全包测试**

```
go test ./cmd/tm/cli/ -v -count=1
golangci-lint run ./cmd/tm/cli/...
```

预期：全 PASS

- [ ] **Step 6.6: `&&` chain atomic commit + 反向白名单 gate**

```bash
git add cmd/tm/cli/session.go cmd/tm/cli/session_test.go && \
  git diff --staged --stat && \
  git status --short && \
  [ -z "$(git diff --name-only --cached | grep -vE '^cmd/tm/cli/session(\.go|_test\.go)$')" ] && \
  git commit -m "Switch RunSession to storage.AggregateUsage push-down API" && \
  git log -1 --stat
```

---

**Phase B 边界**：海盐 gate Tasks 5-6 完整后再启动 Phase C。

---

## Phase C — 顺手补（Task 7-8）

### Task 7: blocks --breakdown 嵌套显示

**Owner:** cloud

**Files:**
- Modify: `internal/blocks/types.go` — SessionBlock 加 PerModel 字段
- Modify: `internal/blocks/burn.go` — Annotate 时填 PerModel
- Modify: `internal/render/types.go` — BlockRow 加 `Breakdown []ModelBreakdown` 字段
- Modify: `cmd/tm/cli/blocks.go` — Breakdown=true 时填 BlockRow.Breakdown
- Modify: `cmd/tm/cli/blocks_test.go` — 加 breakdown 嵌套测试
- Modify: `internal/render/boxed.go` — RenderBlocks 加 if opts.Breakdown 分支
- Modify: `internal/render/json.go` — blockJSONRow 加 ModelBreakdowns 字段

- [ ] **Step 7.1: 写失败 test**

```go
func TestRunBlocksBreakdownNests(t *testing.T) {
	entries := []storage.TokenUsageEntry{
		{
			SessionID: "s1", Timestamp: mustParseTimeT(t, "2026-05-19T10:00:00Z"),
			Model: "claude", InputTokens: 100, CostUSD: 1.0,
		},
		{
			SessionID: "s1", Timestamp: mustParseTimeT(t, "2026-05-19T11:00:00Z"),
			Model: "gpt", InputTokens: 200, CostUSD: 2.0,
		},
	}
	args := cli.BlocksArgs{
		Shared:        cli.Shared{Breakdown: true, JSON: true},
		SessionLength: 5 * time.Hour,
		Now:           mustParseTimeT(t, "2026-05-19T12:00:00Z"),
	}
	var buf bytes.Buffer
	if err := cli.RunBlocks(context.Background(), &buf, args, stubAggregateLoader{rows: entries}); err != nil {
		t.Fatal(err)
	}
	// JSON breakdown should expose modelBreakdowns array
	if !strings.Contains(buf.String(), `"modelBreakdowns"`) {
		t.Errorf("expected modelBreakdowns in JSON output:\n%s", buf.String())
	}
	if !strings.Contains(buf.String(), `"model": "claude"`) {
		t.Errorf("expected claude in breakdown:\n%s", buf.String())
	}
	if !strings.Contains(buf.String(), `"model": "gpt"`) {
		t.Errorf("expected gpt in breakdown:\n%s", buf.String())
	}
}
```

- [ ] **Step 7.2: 跑测试确认 fail**

- [ ] **Step 7.3: 扩 SessionBlock + BlockRow with PerModel / Breakdown**

`internal/blocks/types.go` 加字段：

```go
type SessionBlock struct {
	// ... 已有字段 ...
	PerModel map[string]TokenCounts  // new: per-model accumulator (Breakdown source)
	// ... 已有字段 ...
}
```

`internal/render/types.go` BlockRow 加字段：

```go
type BlockRow struct {
	Period            string
	Models            []string
	InputTokens       int64
	OutputTokens      int64
	CacheCreateTokens int64
	CacheReadTokens   int64
	TotalTokens       int64
	Cost              float64
	Status            string
	Projection        *BlockProjection
	Breakdown         []ModelBreakdown  // new: populated when opts.Breakdown=true
}
```

`internal/blocks/burn.go::Annotate` 修改：在已有的 per-block accumulate loop 里同步填 PerModel：

```go
// 在 Annotate / Identify 的 entry loop 内部，无论原来 b.Tokens.Input += e.InputTokens 是哪一行附近，
// 加入：
if b.PerModel == nil { b.PerModel = map[string]TokenCounts{} }
pm := b.PerModel[e.Model]
pm.Input += e.InputTokens
pm.Output += e.OutputTokens
pm.CacheCreate += e.CacheCreationInputTokens
pm.CacheRead += e.CacheReadInputTokens
b.PerModel[e.Model] = pm
```

具体行号需要 worker 自行定位（grep `b.Tokens.Input +=` 找到 accumulate 位置）。

- [ ] **Step 7.4: cli/blocks.go 填 BlockRow.Breakdown**

`cmd/tm/cli/blocks.go::RunBlocks` 构造 BlockRow 处加：

```go
if args.Shared.Breakdown && len(b.PerModel) > 0 {
	models := make([]string, 0, len(b.PerModel))
	for m := range b.PerModel { models = append(models, m) }
	sort.Strings(models)
	for _, m := range models {
		tc := b.PerModel[m]
		// per-model cost is best approximated proportionally to the block's
		// total cost weighted by token share — we don't have per-entry cost
		// here. Renderer just shows the share; the box footer keeps the
		// authoritative block total.
		share := 0.0
		total := b.Tokens.Total()
		if total > 0 {
			share = b.Cost * float64(tc.Total()) / float64(total)
		}
		row.Breakdown = append(row.Breakdown, render.ModelBreakdown{
			Model:             m,
			InputTokens:       tc.Input,
			OutputTokens:      tc.Output,
			CacheCreateTokens: tc.CacheCreate,
			CacheReadTokens:   tc.CacheRead,
			TotalTokens:       tc.Total(),
			Cost:              share,
		})
	}
}
```

注：cost share 是按 token 占比估算（block-level 已 sum，没有 per-entry cost）。如果用户需要精确 per-model cost，应该用 `tm session --breakdown`。

- [ ] **Step 7.5: render/boxed.go 加 blocks breakdown 嵌套**

`internal/render/boxed.go::RenderBlocks` 在 row loop 内加：

```go
if opts.Breakdown {
	for _, b := range r.Breakdown {
		t.AppendRow(table.Row{
			"  └─ " + b.Model, "", "",
			fmtInt(b.InputTokens), fmtInt(b.OutputTokens),
			fmtInt(b.CacheCreateTokens), fmtInt(b.CacheReadTokens),
			fmtInt(b.TotalTokens), fmtCost(b.Cost), "",
		})
	}
}
```

列数与 RenderBlocks 主行一致（PERIOD/MODELS/2-空-Models/4 token/Cost/STATUS = 10）。

JSON 路径 (`internal/render/json.go::renderBlocksJSON`) 加 `modelBreakdowns` 字段：在 blockJSONRow struct 加 `ModelBreakdowns []breakdownJSON json:"modelBreakdowns,omitempty"`，在 renderBlocksJSON loop 内填充。

- [ ] **Step 7.6: 跑测试确认通过**

```
go test ./cmd/tm/cli/ ./internal/blocks/ ./internal/render/ -v -count=1
golangci-lint run ./...
```

- [ ] **Step 7.7: 集成验证**

```bash
go build -o /tmp/tm-task7 ./cmd/tm
/tmp/tm-task7 blocks --breakdown --json | jq '.blocks[0].modelBreakdowns'  # 应输出 model 数组
```

- [ ] **Step 7.8: `&&` chain atomic commit + 反向白名单 gate**

```bash
git add internal/blocks/types.go internal/blocks/burn.go internal/render/types.go cmd/tm/cli/blocks.go cmd/tm/cli/blocks_test.go internal/render/boxed.go internal/render/json.go && \
  git diff --staged --stat && \
  git status --short && \
  [ -z "$(git diff --name-only --cached | grep -vE '^(internal/blocks/(types|burn)\.go|cmd/tm/cli/blocks(\.go|_test\.go)|internal/render/(types|boxed|json)\.go)$')" ] && \
  git commit -m "Add --breakdown nesting for blocks command" && \
  git log -1 --stat
```

---

### Task 8: AggregateUsage Mode=calculate 集成测试（删冗余 stub）

**Owner:** cloud

**Files:**
- Modify: `cmd/tm/cli/aggregate_test.go`
- Modify: `cmd/tm/cli/session_test.go`

Task 5/6 已经把 Mode=calculate 接到 recalculateCost。本 task 验证逐 model breakdown 路径下 cost 精度。

- [ ] **Step 8.1: 加 integration test**

```go
func TestRunAggregateModeCalculateBreakdownPrecision(t *testing.T) {
	// SUM-then-reprice path: storage returns per-model aggregated rows
	// (Breakdown=true), cli recalculates each model's cost.
	rows := []storage.AggregateUsageRow{
		{Bucket: "2026-05-19", Model: "claude-opus-4-7", InputTokens: 1_000_000, Cost: 0},
		{Bucket: "2026-05-19", Model: "gpt-5", InputTokens: 1_000_000, Cost: 0},
	}
	args := cli.AggregateArgs{
		Shared: cli.Shared{Mode: "calculate", Breakdown: true, JSON: true},
		Bucket: cli.BucketDaily,
	}
	var buf bytes.Buffer
	if err := cli.RunAggregate(context.Background(), &buf, args, stubAggregateLoaderPushdown{rows: rows}); err != nil {
		t.Fatal(err)
	}
	// Both per-model breakdown rows should have non-zero costs (recalculated)
	if !strings.Contains(buf.String(), `"model": "claude-opus-4-7"`) {
		t.Errorf("expected claude breakdown:\n%s", buf.String())
	}
	if !strings.Contains(buf.String(), `"model": "gpt-5"`) {
		t.Errorf("expected gpt breakdown:\n%s", buf.String())
	}
	if strings.Contains(buf.String(), `"totalCost": 0`) {
		t.Errorf("mode=calculate should recompute non-zero cost:\n%s", buf.String())
	}
}
```

- [ ] **Step 8.2: 跑测试确认通过**

```
go test ./cmd/tm/cli/ -run TestRunAggregateModeCalculateBreakdownPrecision -v
```

- [ ] **Step 8.3: `&&` chain atomic commit**

```bash
git add cmd/tm/cli/aggregate_test.go && \
  git diff --staged --stat && \
  git status --short && \
  [ -z "$(git diff --name-only --cached | grep -vE '^cmd/tm/cli/aggregate_test\.go$')" ] && \
  git commit -m "Verify Mode=calculate precision in breakdown path" && \
  git log -1 --stat
```

---

## Phase D — Benchmark + Baseline（Task 9-10）

### Task 9: storage benchmark

**Owner:** star

**Files:**
- Create: `internal/storage/aggregate_bench_test.go`

- [ ] **Step 9.1: 写 benchmark 文件**

```go
package storage

import (
	"context"
	"fmt"
	"testing"
	"time"
)

func benchSeed(b *testing.B, db *DB, n int) {
	b.Helper()
	start := time.Date(2026, 1, 1, 0, 0, 0, 0, time.UTC)
	for i := 0; i < n; i++ {
		sid := fmt.Sprintf("sess-%d", i%500)
		_ = db.UpsertSession(sid, "claude", start)
		ts := start.Add(time.Duration(i) * time.Minute)
		_ = db.InsertTokenUsage("", sid, 1000, 500, 100, 200, "claude", 0.01, ts, fmt.Sprintf("src-%d", i))
	}
}

func benchOpen(b *testing.B) *DB {
	b.Helper()
	path := b.TempDir() + "/bench.db"
	db, err := Open(path)
	if err != nil { b.Fatal(err) }
	b.Cleanup(func() { db.Close() })
	return db
}

func BenchmarkAggregateUsageDay10k(b *testing.B) {
	db := benchOpen(b)
	benchSeed(b, db, 10_000)
	b.ResetTimer()
	for i := 0; i < b.N; i++ {
		_, _ = db.AggregateUsage(context.Background(), AggregateFilter{Bucket: BucketDay})
	}
}

func BenchmarkAggregateUsageDay100k(b *testing.B) {
	db := benchOpen(b)
	benchSeed(b, db, 100_000)
	b.ResetTimer()
	for i := 0; i < b.N; i++ {
		_, _ = db.AggregateUsage(context.Background(), AggregateFilter{Bucket: BucketDay})
	}
}

func BenchmarkAggregateUsageSession100k(b *testing.B) {
	db := benchOpen(b)
	benchSeed(b, db, 100_000)
	b.ResetTimer()
	for i := 0; i < b.N; i++ {
		_, _ = db.AggregateUsage(context.Background(), AggregateFilter{Bucket: BucketSession})
	}
}

func BenchmarkAggregateUsageDayBreakdown100k(b *testing.B) {
	db := benchOpen(b)
	benchSeed(b, db, 100_000)
	b.ResetTimer()
	for i := 0; i < b.N; i++ {
		_, _ = db.AggregateUsage(context.Background(), AggregateFilter{
			Bucket: BucketDay, Breakdown: true,
		})
	}
}
```

- [ ] **Step 9.2: 跑 benchmark**

```bash
go test -bench=BenchmarkAggregateUsage -benchmem -count=3 ./internal/storage/ > /tmp/bench-results.txt
cat /tmp/bench-results.txt
```

每个 case 跑 3 次，取最低 ns/op。

- [ ] **Step 9.3: `&&` chain atomic commit**

```bash
git add internal/storage/aggregate_bench_test.go && \
  git diff --staged --stat && \
  git status --short && \
  [ -z "$(git diff --name-only --cached | grep -vE '^internal/storage/aggregate_bench_test\.go$')" ] && \
  git commit -m "Add AggregateUsage benchmark for Day/Session/Breakdown paths" && \
  git log -1 --stat
```

---

### Task 10: 真实 DB measurement + baseline 报告

**Owner:** star

**Files:**
- Create: `docs/perf/v1.0.2-baseline.md`

- [ ] **Step 10.1: build + 跑真实 DB**

```bash
go build -o /tmp/tm-v1.0.2 ./cmd/tm

# warm-up
/tmp/tm-v1.0.2 daily > /dev/null

# measure
for cmd in "daily" "weekly" "monthly" "session" "blocks"; do
  echo "=== $cmd ==="
  for i in 1 2 3; do
    /usr/bin/time -p /tmp/tm-v1.0.2 $cmd > /dev/null
  done
done 2>&1 | tee /tmp/v1.0.2-timings.txt

# pprof for daily (verify push-down is taking effect)
/tmp/tm-v1.0.2 daily --cpu-profile /tmp/v1.0.2-baseline.pprof > /dev/null
go tool pprof -top -cum /tmp/v1.0.2-baseline.pprof | head -30 > /tmp/v1.0.2-pprof.txt
```

- [ ] **Step 10.2: 写 baseline 报告**

`docs/perf/v1.0.2-baseline.md`：

```markdown
# v1.0.2 性能基线 — SQL Push-Down 验证

对照 v1.0.1 baseline (`docs/perf/v1.0.1-baseline.md`) 验证 push-down 优化效果。
同一开发机 / 同一 DB / 同一测量协议。

## 测试环境
- 同 v1.0.1：macOS 25.x · Apple Silicon · Go 1.24
- DB: `~/.agmon/data/agmon.db` (1587 sessions / 445,904 token_usage rows / 549 MB)

## 命令耗时对比

| 命令 | v1.0.1 warm | v1.0.2 warm | 倍率 | 目标 | 通过 |
| --- | --- | --- | --- | --- | --- |
| tm daily | 820 ms | <measured> ms | <X>x | <250 ms | <✓/✗> |
| tm weekly | <prior> | <measured> | <X>x | <250 ms | <✓/✗> |
| tm monthly | <prior> | <measured> | <X>x | <250 ms | <✓/✗> |
| tm session | <prior> | <measured> | <X>x | <300 ms | <✓/✗> |
| tm blocks | ~820 ms | <measured> | ~1x | 不退步 | <✓/✗> |

## pprof Top 10（cumulative）

\`\`\`
<paste of go tool pprof -top -cum head -30>
\`\`\`

## 与 v1.0.1 的 pprof 对比

- 旧 top: `_sqlite3VdbeExec` (72%) + `Pread` (71%) — 全表 scan 444k 行
- 新 top: <hotspot>

## 内部 benchmark 数字

\`\`\`
<paste of go test -bench output>
\`\`\`

## 结论

<填实测后的总结。如果命中目标，说明 push-down 战略成功；如果未命中，分析下一步>
```

把实测数字填进 `<...>` 占位。

- [ ] **Step 10.3: `&&` chain atomic commit**

```bash
git add docs/perf/v1.0.2-baseline.md && \
  git diff --staged --stat && \
  git status --short && \
  [ -z "$(git diff --name-only --cached | grep -vE '^docs/perf/v1\.0\.2-baseline\.md$')" ] && \
  git commit -m "Add v1.0.2 push-down baseline report" && \
  git log -1 --stat
```

---

## Phase E — 文档 + Release（Task 11-12）

### Task 11: CHANGELOG + MIGRATION

**Owner:** cloud

**Files:**
- Modify: `CHANGELOG.md`
- Modify: `docs/MIGRATION-v1.0.md`

- [ ] **Step 11.1: 写 CHANGELOG v1.0.2 段**

在 CHANGELOG.md 顶部插入：

```markdown
## v1.0.2 — YYYY-MM-DD

### Performance
- SQL push-down for `daily` / `weekly` / `monthly` / `session`: warm \<v1.0.1>ms → \<v1.0.2>ms (\<X>×)
- `blocks` command unchanged (5h identification needs raw timeline)
- See `docs/perf/v1.0.2-baseline.md` for full benchmark

### Added
- `blocks --breakdown` 嵌套显示 per-model 子行（v1.0.1 known limitation 补齐）
- `session --json` 输出含 `projectPath` / `lastActivity` 字段（来自 SQL `MAX(s.cwd)` / `MAX(u.timestamp)`）

### Changed
- `Mode=calculate` 在 push-down 路径下按 bucket SUM 后重算（精度与逐 entry 重算差 <$0.01）。多模型 bucket 用 Models[0] 估算；精确 per-model 走 `--breakdown`。
```

发布前把 YYYY-MM-DD 换今天日期。

- [ ] **Step 11.2: 写 MIGRATION 段**

`docs/MIGRATION-v1.0.md` 加：

```markdown
## v1.0.2 changes

### Mode=calculate 精度变化（潜在小差异）

v1.0.1：cli 层逐 entry 重算 cost
v1.0.2：cli 层按 SQL 聚合 row 重算 cost（基于 SUM token + Models[0] 单价）

实践影响：单模型 bucket 无差异；混合模型 bucket（同一天用了 claude + gpt）的 cost 估算会有 < $0.01 量级差异。需要精确 per-model cost 时改用 `--breakdown`。

### blocks --breakdown 新功能

之前 `--breakdown` 在 blocks 命令无效（v1.0.1 known limitation）。v1.0.2 嵌套显示每 block 的 per-model token + cost share（按 token 占比估算）。
```

- [ ] **Step 11.3: `&&` chain atomic commit**

```bash
git add CHANGELOG.md docs/MIGRATION-v1.0.md && \
  git diff --staged --stat && \
  git status --short && \
  [ -z "$(git diff --name-only --cached | grep -vE '^(CHANGELOG\.md|docs/MIGRATION-v1\.0\.md)$')" ] && \
  git commit -m "Document v1.0.2 SQL push-down + blocks breakdown" && \
  git log -1 --stat
```

---

### Task 12: 全套 ship sanity

**Owner:** cloud

**Files:** 无修改

- [ ] **Step 12.1: 跑全套 sanity**

```bash
go test ./... -count=1
golangci-lint run
go build -o tm ./cmd/tm

# v1.0.1 → v1.0.2 行为兼容性（输出格式应完全一致）
./tm daily | head -5
./tm daily --json | jq '.totals'
./tm daily --breakdown | head -10
./tm daily --order desc | head -5
./tm daily --until 20260520 | grep 2026-05-20
./tm daily --timezone Asia/Shanghai | head -5
./tm daily --no-color | head -3
./tm | head -5  # default daily
./tm session | head -5
./tm session --json | jq '.sessions[0] | keys'  # 应含 projectPath / lastActivity
./tm blocks | head -10
./tm blocks --breakdown | head -10  # 新：含 └─ <model> 行
./tm blocks --json | jq '.blocks[0] | keys'
```

每条命令输出贴一行关键截图作为 ship gate 证据。

- [ ] **Step 12.2: team report 全套 sanity 输出**

把 14 条命令的输出第一行 + jq 结果发给 reviewer 作为 final gate 证据。

---

## 验收 Gate（reviewer 海盐最终检查）

1. `go test ./...` 全绿
2. `golangci-lint run` 0 issues
3. 13 条手测命令全部输出符合 spec
4. `tm daily` warm <250ms（验收目标 #1）
5. `tm session` warm <300ms（验收目标 #2）
6. `tm weekly` / `tm monthly` warm <250ms（验收目标 #3）
7. `tm blocks` warm 不退步（验收目标 #4）
8. `docs/perf/v1.0.2-baseline.md` 含完整对比 + pprof top10
9. CHANGELOG / MIGRATION 段落齐全
10. 当天日期写入 CHANGELOG `v1.0.2 — YYYY-MM-DD`

通过后：
```bash
git tag -a v1.0.2 -m "v1.0.2 — SQL push-down performance"
git push origin main v1.0.2
```

CI 跑通 → goreleaser 发 6 平台 binary。

---

## 工程纪律（v1.0.1 协议延续）

- `git add` 显式路径，不 `git add .` / `-A`
- `&&` chain atomic commit + 反向白名单 gate
- commit message 无 Claude 品牌、无 Co-Authored-By
- 不 skip hooks（`--no-verify`）、不 force push、不改 git config
- 包内串行，包外并发：
  - `internal/storage/aggregate*.go` 由 star 独占（Task 1-4, 9-10）
  - `cmd/tm/cli/aggregate*.go` 由 cloud 独占（Task 5, 8）
  - `cmd/tm/cli/session*.go` 由 cloud 独占（Task 6）
  - `internal/blocks/{types,burn}.go` + `cmd/tm/cli/blocks*.go` + `internal/render/{boxed,json}.go` 由 cloud Task 7 串行做
  - `CHANGELOG.md` / `docs/MIGRATION-v1.0.md` / `docs/perf/v1.0.2-baseline.md` 由 cloud Task 11 + star Task 10
- Task 5/6 必须在 Task 1-4 全 ACCEPT 之后开始（接口契约依赖）
- Task 9/10 在 Phase B 完成后做（要测真实 push-down 效果）
- Task 7/8 与 Phase D 可以并行（不撞文件）

---

## 总计

12 个 task。预计 1 天双 worker 并行完工。CI / goreleaser 已就绪。
