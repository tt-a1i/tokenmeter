# v1.0.1 — ccusage 对齐 Part 1 实施 Plan

> **For Hive workers (star / cloud / reviewer):** 这是 v1.0.1 的执行清单。star 负责 cli/* + cmd/tm/main.go，cloud 负责 internal/render/* + 文档 + profile。每个 task 走 TDD：先写失败 test → 跑确认失败 → 写最小实现 → 跑确认通过 → commit。Reviewer 在 Phase 边界（A 末 / B 末 / D 末）gate。

**Goal:** 把 v1.0.0 漏接的 CLI flag 全部接通，让输出表格 + JSON 与 ccusage v20 视觉/字段对齐，建立性能基线。

**Architecture:** 新增 `internal/render/` 单一渲染层（表格 + JSON），把 cli/{aggregate,session,blocks}.go 的输出逻辑全收上来；flag 实际值在 RunXxx 中按 `Shared.*` 字段消费；`--until` 在 parseDateFlag 后 +24h 实现闭区间。

**Tech Stack:** Go 1.24 · `github.com/jedib0t/go-pretty/v6/table` (新依赖) · `golang.org/x/term`（isatty 检测）· `runtime/pprof`（profile）

**Spec:** `docs/superpowers/specs/2026-05-20-v1.0.1-ccusage-alignment-part1-design.md`

---

## 文件结构（决策锁定）

新建：
- `internal/render/types.go` — `AggregateRow` / `ModelBreakdown` / `SessionRow` / `BlockRow` / `Options`
- `internal/render/color.go` — 颜色 detection（isatty + NO_COLOR + --no-color + JSON mode 抑制）
- `internal/render/boxed.go` — 圆角边框表格 + TOTAL 汇总 + Breakdown 嵌套
- `internal/render/json.go` — ccusage camelCase JSON 顶层结构
- `internal/render/render_test.go` — 渲染层单元测试
- `docs/perf/v1.0.1-baseline.md` — 性能 baseline 报告

修改：
- `cmd/tm/cli/cli.go` — 加 `--cpu-profile` hidden flag；parseDateFlag --until +24h
- `cmd/tm/cli/aggregate.go` — 接通 Order/Mode/Timezone/Breakdown；切到 render 包
- `cmd/tm/cli/session.go` — 同上
- `cmd/tm/cli/blocks.go` — 接通 --mode；切到 render 包
- `cmd/tm/cli/router.go` — 无参 → 默认 daily
- `cmd/tm/cli/statusline.go` — 接通 --no-color/--mode
- `cmd/tm/main.go` — main 路由处理 `tm`（无参）；启动 CPU profile
- `CHANGELOG.md` — v1.0.1 段
- `docs/MIGRATION-v1.0.md` — v1.0.1 changes（--until 闭区间语义）
- `go.mod` / `go.sum` — go-pretty 依赖

---

## Phase A — flag 接通（Task 1-5）

并发策略：star 抢 Task 1-4，cloud 抢 Task 5。包内串行。

### Task 1: `cli/aggregate.go` 接通 Order / Mode / Timezone / Breakdown

**Owner:** star

**Files:**
- Modify: `cmd/tm/cli/aggregate.go`
- Test: `cmd/tm/cli/aggregate_test.go`

- [ ] **Step 1.1: 写失败 test — order desc**

```go
// in aggregate_test.go, add:
func TestRunAggregateOrderDesc(t *testing.T) {
	entries := []storage.TokenUsageEntry{
		{SessionID: "s1", Timestamp: mustTime("2026-05-19T10:00:00Z"), InputTokens: 100, Model: "claude"},
		{SessionID: "s2", Timestamp: mustTime("2026-05-20T10:00:00Z"), InputTokens: 200, Model: "claude"},
	}
	args := AggregateArgs{
		Shared: Shared{Order: "desc"},
		Bucket: BucketDaily,
	}
	var buf bytes.Buffer
	err := RunAggregate(context.Background(), &buf, args, stubLoader{entries: entries})
	if err != nil { t.Fatal(err) }
	out := buf.String()
	idx20 := strings.Index(out, "2026-05-20")
	idx19 := strings.Index(out, "2026-05-19")
	if idx20 == -1 || idx19 == -1 || idx20 > idx19 {
		t.Fatalf("expected 2026-05-20 before 2026-05-19, got:\n%s", out)
	}
}
```

- [ ] **Step 1.2: 跑测试确认失败**

```
go test ./cmd/tm/cli/ -run TestRunAggregateOrderDesc -v
```
预期：FAIL（当前 RunAggregate 永远 `sort.Strings(keys)` 升序）

- [ ] **Step 1.3: 接通 Order 字段**

在 `cmd/tm/cli/aggregate.go` 的 `RunAggregate` 中，将
```go
sort.Strings(keys)
```
替换为：
```go
if a.Shared.Order == "desc" {
	sort.Sort(sort.Reverse(sort.StringSlice(keys)))
} else {
	sort.Strings(keys)
}
```

- [ ] **Step 1.4: 跑测试确认通过**

```
go test ./cmd/tm/cli/ -run TestRunAggregateOrderDesc -v
```

- [ ] **Step 1.5: 写失败 test — timezone 影响 bucket 分组**

```go
func TestRunAggregateTimezoneBucketing(t *testing.T) {
	// 23:30 UTC = 07:30 next day in Asia/Shanghai
	entries := []storage.TokenUsageEntry{
		{SessionID: "s1", Timestamp: mustTime("2026-05-19T23:30:00Z"), InputTokens: 100, Model: "claude"},
	}
	args := AggregateArgs{
		Shared: Shared{Timezone: "Asia/Shanghai"},
		Bucket: BucketDaily,
	}
	var buf bytes.Buffer
	if err := RunAggregate(context.Background(), &buf, args, stubLoader{entries: entries}); err != nil {
		t.Fatal(err)
	}
	if !strings.Contains(buf.String(), "2026-05-20") {
		t.Fatalf("expected 2026-05-20 bucket (Shanghai TZ), got:\n%s", buf.String())
	}
}
```

- [ ] **Step 1.6: 跑测试确认失败**

预期：FAIL（当前 bucketKey 用 UTC）

- [ ] **Step 1.7: 接通 Timezone**

在 `RunAggregate` 中解析 timezone：
```go
loc := time.UTC
if a.Shared.Timezone != "" {
	parsed, err := time.LoadLocation(a.Shared.Timezone)
	if err != nil {
		return fmt.Errorf("invalid timezone %q: %w", a.Shared.Timezone, err)
	}
	loc = parsed
}
```
将 `groupBy(entries, a.Bucket)` 改成 `groupBy(entries, a.Bucket, loc)`，签名同步改：
```go
func groupBy(entries []storage.TokenUsageEntry, b Bucket, loc *time.Location) map[string]*aggGroup {
	// ...
	key := bucketKey(e.Timestamp.In(loc), b)
	// ...
}
```

- [ ] **Step 1.8: 跑测试确认通过**

```
go test ./cmd/tm/cli/ -run TestRunAggregateTimezoneBucketing -v
```

- [ ] **Step 1.9: 写失败 test — mode applies pricing**

```go
func TestRunAggregateModeCalculate(t *testing.T) {
	// CostUSD=0 + InputTokens>0 + Mode=calculate => pricing should recompute non-zero
	entries := []storage.TokenUsageEntry{
		{SessionID: "s1", Timestamp: mustTime("2026-05-19T10:00:00Z"),
		 InputTokens: 1_000_000, OutputTokens: 100_000, Model: "claude-opus-4-7", CostUSD: 0},
	}
	args := AggregateArgs{
		Shared: Shared{Mode: "calculate"},
		Bucket: BucketDaily,
	}
	var buf bytes.Buffer
	if err := RunAggregate(context.Background(), &buf, args, stubLoader{entries: entries}); err != nil {
		t.Fatal(err)
	}
	if strings.Contains(buf.String(), "$0.00") {
		t.Fatalf("mode=calculate should recompute cost, got:\n%s", buf.String())
	}
}
```

- [ ] **Step 1.10: 跑测试确认失败**

预期：FAIL（当前 RunAggregate 忽略 Mode）

- [ ] **Step 1.11: 接通 Mode**

在 `RunAggregate` groupBy 之前对 entries 应用 pricing：
```go
import "github.com/tt-a1i/tokenmeter/internal/pricing"

mode, err := pricing.ParseMode(a.Shared.Mode)
if err != nil { return err }
entries = pricing.Apply(entries, mode)
```

（如果 `pricing.Apply` 签名不接受 `[]TokenUsageEntry`，先调整 `internal/pricing/mode.go` 把 Apply 改成 generic 接口或新增 `ApplyEntries(entries []storage.TokenUsageEntry, mode CostMode) []storage.TokenUsageEntry`。如果 internal/pricing 不能 import internal/storage，则在 cli 层 inline 转换 + 调用 `pricing.CalculateCost`。）

- [ ] **Step 1.12: 跑测试确认通过**

- [ ] **Step 1.13: 写失败 test — breakdown 内部计算**

```go
func TestRunAggregateBreakdownPropagates(t *testing.T) {
	entries := []storage.TokenUsageEntry{
		{SessionID: "s1", Timestamp: mustTime("2026-05-19T10:00:00Z"),
		 InputTokens: 100, Model: "claude-opus-4-7", CostUSD: 1},
		{SessionID: "s1", Timestamp: mustTime("2026-05-19T11:00:00Z"),
		 InputTokens: 200, Model: "gpt-5.1-codex", CostUSD: 2},
	}
	args := AggregateArgs{
		Shared: Shared{Breakdown: true},
		Bucket: BucketDaily,
	}
	var buf bytes.Buffer
	if err := RunAggregate(context.Background(), &buf, args, stubLoader{entries: entries}); err != nil {
		t.Fatal(err)
	}
	// Phase A 只验证 Breakdown 被读取（暂用 stderr 或不影响主表的方式标记）；
	// 完整 render 留到 Task 11 切换 render 包。这里用一个 side effect：
	// 当 Breakdown=true 时，groupBy 必须在 aggGroup 里填 perModel map。
	// 改 aggGroup 加 perModel 字段，本测试验证它存在。
	t.Skip("structural test — coverage moves to render layer in Task 11")
}
```

实际策略：Task 1 阶段为 Breakdown 在 `aggGroup` 添加 `perModel map[string]*modelStats` 字段并填充；表格输出仍是 4 列（render 切换在 Task 11 完成）。

```go
type modelStats struct {
	Input, Output, CacheCreate, CacheRead int64
	Cost float64
}

type aggGroup struct {
	tokens int64
	cost   float64
	models []string
	seen   map[string]struct{}
	input, output, cacheCreate, cacheRead int64
	perModel map[string]*modelStats
}
```

在 groupBy 累加循环里更新这些字段（input/output/cacheCreate/cacheRead）和 perModel（不论 Breakdown 是否为 true 都填充——render 层按需读）。

- [ ] **Step 1.14: 跑全包测试 + lint**

```
go test ./cmd/tm/cli/ -v
golangci-lint run ./cmd/tm/cli/...
```

- [ ] **Step 1.15: Commit**

```bash
git add cmd/tm/cli/aggregate.go cmd/tm/cli/aggregate_test.go
git status --short
git diff --staged --stat
git commit -m "Wire Order/Mode/Timezone/Breakdown into RunAggregate"
```

---

### Task 2: `cli/session.go` 接通同上 flag

**Owner:** star

**Files:**
- Modify: `cmd/tm/cli/session.go`
- Test: `cmd/tm/cli/session_test.go`

- [ ] **Step 2.1: 写失败 test — order desc / mode / 同 Task 1 思路**

```go
func TestRunSessionOrderDesc(t *testing.T) {
	entries := []storage.TokenUsageEntry{
		{SessionID: "aaa", Timestamp: mustTime("2026-05-19T10:00:00Z"), Model: "claude"},
		{SessionID: "zzz", Timestamp: mustTime("2026-05-19T11:00:00Z"), Model: "claude"},
	}
	args := SessionArgs{Shared: Shared{Order: "desc"}}
	var buf bytes.Buffer
	if err := RunSession(context.Background(), &buf, args, stubLoader{entries: entries}); err != nil {
		t.Fatal(err)
	}
	idxZ := strings.Index(buf.String(), "zzz")
	idxA := strings.Index(buf.String(), "aaa")
	if idxZ == -1 || idxA == -1 || idxZ > idxA {
		t.Fatalf("expected zzz before aaa, got:\n%s", buf.String())
	}
}
```

- [ ] **Step 2.2: 跑测试确认失败**

- [ ] **Step 2.3: 接通 Order/Mode/Breakdown（同 Task 1 风格）**

在 `RunSession` 中：
```go
mode, err := pricing.ParseMode(a.Shared.Mode)
if err != nil { return err }
entries = pricing.ApplyEntries(entries, mode)
// ... existing group loop ...
sort.Strings(keys)
if a.Shared.Order == "desc" {
	sort.Sort(sort.Reverse(sort.StringSlice(keys)))
}
```

aggGroup 复用 Task 1 加的 perModel/input/output/cacheCreate/cacheRead 字段。

- [ ] **Step 2.4: 跑测试确认通过**

- [ ] **Step 2.5: Commit**

```bash
git add cmd/tm/cli/session.go cmd/tm/cli/session_test.go
git status --short
git diff --staged --stat
git commit -m "Wire Order/Mode/Breakdown into RunSession"
```

---

### Task 3: `cli/blocks.go` 接通 --mode

**Owner:** star

**Files:**
- Modify: `cmd/tm/cli/blocks.go`
- Test: `cmd/tm/cli/blocks_test.go`

`--json` 在 v1.0 已接通；本 task 只补 `--mode`。

- [ ] **Step 3.1: 写失败 test — mode=calculate 重算 cost**

```go
func TestRunBlocksModeCalculate(t *testing.T) {
	entries := []storage.TokenUsageEntry{
		{SessionID: "s", Timestamp: mustTime("2026-05-19T10:00:00Z"),
		 InputTokens: 1_000_000, Model: "claude-opus-4-7", CostUSD: 0},
	}
	args := BlocksArgs{
		Shared:        Shared{Mode: "calculate", JSON: true},
		SessionLength: 5 * time.Hour,
		Now:           mustTime("2026-05-19T12:00:00Z"),
	}
	var buf bytes.Buffer
	if err := RunBlocks(context.Background(), &buf, args, stubLoader{entries: entries}); err != nil {
		t.Fatal(err)
	}
	// JSON output should include non-zero cost
	if strings.Contains(buf.String(), `"cost":0`) || strings.Contains(buf.String(), `"cost": 0`) {
		t.Fatalf("expected recalculated cost, got:\n%s", buf.String())
	}
}
```

- [ ] **Step 3.2: 跑测试确认失败**

- [ ] **Step 3.3: 接通 Mode**

在 `RunBlocks` 中：
```go
mode, err := pricing.ParseMode(args.Shared.Mode)
if err != nil { return err }
entries = pricing.ApplyEntries(entries, mode)
```
插在 `loader.ListUsageForBlocksFiltered` 之后、`blocks.Annotate` 之前。

- [ ] **Step 3.4: 跑测试确认通过**

- [ ] **Step 3.5: Commit**

```bash
git add cmd/tm/cli/blocks.go cmd/tm/cli/blocks_test.go
git status --short
git diff --staged --stat
git commit -m "Wire --mode into RunBlocks"
```

---

### Task 4: `cli/router.go` + `cmd/tm/main.go` — `tm` 无参 默认 daily

**Owner:** star

**Files:**
- Modify: `cmd/tm/cli/router.go`
- Modify: `cmd/tm/main.go`
- Test: `cmd/tm/cli/router_test.go`

- [ ] **Step 4.1: 写失败 test — Route([]) 返回 daily 而非 help**

```go
func TestRouteNoArgsDefaultsToDaily(t *testing.T) {
	cmd, err := Route(nil)
	if err != nil { t.Fatal(err) }
	if cmd.Name != "daily" {
		t.Fatalf("expected default 'daily', got %q", cmd.Name)
	}
	if cmd.AggregateArgs.Bucket != BucketDaily {
		t.Fatalf("expected BucketDaily, got %v", cmd.AggregateArgs.Bucket)
	}
}
```

- [ ] **Step 4.2: 跑测试确认失败**

- [ ] **Step 4.3: 修 Route**

`cmd/tm/cli/router.go` 顶部分支改：
```go
if len(argv) == 0 {
	return Command{
		Name:          "daily",
		AggregateArgs: AggregateArgs{Bucket: BucketDaily},
	}, nil
}
```

- [ ] **Step 4.4: 修 main.go**

`cmd/tm/main.go` 主分支：把 `tm`（无参）从打 help 改成 dispatch daily。具体位置在 `main()` 顶部判 `len(os.Args) == 1` 那块，改成调用 `runCLIDispatch(nil)`。同步搜索 `printHelp()` 默认调用，确保只有 `tm help` / `tm -h` / `tm --help` 进 printHelp。

- [ ] **Step 4.5: 集成测试**

```bash
go build -o tm ./cmd/tm
./tm > /tmp/out.txt 2>&1
grep -q "^DATE" /tmp/out.txt || (echo "FAIL: tm with no args should print daily" && exit 1)
```

- [ ] **Step 4.6: Commit**

```bash
git add cmd/tm/cli/router.go cmd/tm/cli/router_test.go cmd/tm/main.go
git status --short
git diff --staged --stat
git commit -m "Default tm with no args to daily report"
```

---

### Task 5: `cli/statusline.go` 接通 --no-color / --mode

**Owner:** cloud

**Files:**
- Modify: `cmd/tm/cli/statusline.go`
- Test: `cmd/tm/cli/statusline_test.go`

- [ ] **Step 5.1: 读现状**

```bash
sed -n '1,80p' cmd/tm/cli/statusline.go
```
确认 RunStatusline 当前忽略 `--no-color` / `--mode`。

- [ ] **Step 5.2: 写失败 test — --no-color**

```go
func TestRunStatuslineNoColor(t *testing.T) {
	// stub reader returns active block; assert output does NOT contain ANSI ESC
	var buf bytes.Buffer
	in := strings.NewReader(`{}`)
	cfg := t.TempDir() + "/statusline.json"
	now := mustTime("2026-05-19T12:00:00Z")
	// Build a shared with NoColor=true; statusline reads Shared off some path
	// in main.go — adjust the RunStatusline signature to accept Shared if it
	// doesn't already.
	err := RunStatusline(context.Background(), in, &buf,
		stubActiveBlock(/* one block */), cfg, now,
		WithStatuslineOptions(StatuslineOptions{NoColor: true, Mode: "auto"}))
	if err != nil { t.Fatal(err) }
	if bytes.Contains(buf.Bytes(), []byte{0x1b}) {
		t.Fatalf("--no-color should suppress ANSI escape, got:\n%s", buf.String())
	}
}
```

- [ ] **Step 5.3: 跑测试确认失败**

- [ ] **Step 5.4: 加 StatuslineOptions 类型 + With… helper**

`cmd/tm/cli/statusline.go`：

```go
type StatuslineOptions struct {
	NoColor bool
	Mode    string
}

type statuslineOpt func(*StatuslineOptions)

func WithStatuslineOptions(o StatuslineOptions) statuslineOpt {
	return func(dst *StatuslineOptions) { *dst = o }
}
```

`RunStatusline` 签名增加 `opts ...statuslineOpt`，在 Render 之前根据 NoColor 调用 `internal/statusline` 的关色 API（或在 cli 层 strip ANSI）。Mode 传给 pricing.Apply（如果 statusline 内部有 cost 计算）。

main.go 中调用处补：
```go
return cli.RunStatusline(ctx, os.Stdin, os.Stdout, adapter, cfgPath, now,
	cli.WithStatuslineOptions(cli.StatuslineOptions{
		NoColor: cmd.Shared.NoColor,
		Mode:    cmd.Shared.Mode,
	}))
```

- [ ] **Step 5.5: 跑测试确认通过**

- [ ] **Step 5.6: Commit**

```bash
git add cmd/tm/cli/statusline.go cmd/tm/cli/statusline_test.go cmd/tm/main.go
git status --short
git diff --staged --stat
git commit -m "Wire --no-color and --mode into statusline"
```

---

**Phase A 边界**：Reviewer 在此点检查 Tasks 1-5 是否全 ACCEPT，golangci-lint 0 issues，全 test 绿。通过后再启动 Phase B。

---

## Phase B — 输出对齐 ccusage（Task 6-12）

新增 `internal/render/` 单一包。cloud 独占，避免与 star 在 cmd/tm/cli/ 撞文件。

### Task 6: `internal/render/types.go` + 引入 go-pretty

**Owner:** cloud

**Files:**
- Create: `internal/render/types.go`
- Modify: `go.mod` / `go.sum`

- [ ] **Step 6.1: 引入依赖**

```bash
go get github.com/jedib0t/go-pretty/v6@latest
go mod tidy
```

- [ ] **Step 6.2: 写类型文件**

`internal/render/types.go`：

```go
// Package render emits aggregate / session / block reports in either
// boxed-table or JSON form, with column layout and field naming aligned
// to ccusage v20.
package render

import (
	"io"
	"time"
)

// AggregateRow is one row of daily/weekly/monthly output.
type AggregateRow struct {
	Bucket            string // "2026-05-19" / "2026-W21" / "2026-05"
	Models            []string
	InputTokens       int64
	OutputTokens      int64
	CacheCreateTokens int64
	CacheReadTokens   int64
	TotalTokens       int64
	Cost              float64
	Breakdown         []ModelBreakdown
}

// ModelBreakdown is the per-model substructure under one AggregateRow / SessionRow.
type ModelBreakdown struct {
	Model             string
	InputTokens       int64
	OutputTokens      int64
	CacheCreateTokens int64
	CacheReadTokens   int64
	TotalTokens       int64
	Cost              float64
}

// SessionRow mirrors AggregateRow but is keyed by session id and carries
// last-activity and project-path metadata.
type SessionRow struct {
	SessionID         string
	ProjectPath       string
	LastActivity      time.Time
	Models            []string
	InputTokens       int64
	OutputTokens      int64
	CacheCreateTokens int64
	CacheReadTokens   int64
	TotalTokens       int64
	Cost              float64
	Breakdown         []ModelBreakdown
}

// BlockRow mirrors blocks.SessionBlock in render-layer-friendly form.
type BlockRow struct {
	Period            string // "2026-05-19 10:00"
	Models            []string
	InputTokens       int64
	OutputTokens      int64
	CacheCreateTokens int64
	CacheReadTokens   int64
	TotalTokens       int64
	Cost              float64
	Status            string // "ACTIVE" | "closed" | "gap"
	Projection        *BlockProjection
}

type BlockProjection struct {
	TotalTokens   int64
	TotalCost     float64
	RemainingTime time.Duration
}

// Options controls rendering knobs.
type Options struct {
	Color     bool
	Breakdown bool
	JSON      bool
}

// Renderer is the entrypoint used by cli/* handlers.
type Renderer interface {
	RenderAggregate(w io.Writer, kind string, rows []AggregateRow, opts Options) error
	RenderSessions(w io.Writer, rows []SessionRow, opts Options) error
	RenderBlocks(w io.Writer, rows []BlockRow, opts Options) error
}

// New returns a Renderer that dispatches to either boxed-table or JSON
// output based on opts.JSON.
func New() Renderer { return defaultRenderer{} }

type defaultRenderer struct{}
```

留 Render* 方法的具体实现给 Task 8/9。本 task 只确保类型 + 包能编译。

- [ ] **Step 6.3: 编译通过**

```bash
go build ./internal/render/
```

- [ ] **Step 6.4: Commit**

```bash
git add internal/render/types.go go.mod go.sum
git status --short
git diff --staged --stat
git commit -m "Add render package types + go-pretty dependency"
```

---

### Task 7: `internal/render/color.go` — 颜色 detection

**Owner:** cloud

**Files:**
- Create: `internal/render/color.go`
- Create: `internal/render/color_test.go`

- [ ] **Step 7.1: 写失败 test**

`internal/render/color_test.go`：

```go
package render

import (
	"bytes"
	"os"
	"testing"
)

func TestResolveColorJSONForcesOff(t *testing.T) {
	if Resolve(true, false, &bytes.Buffer{}) != false {
		t.Fatal("JSON mode must disable color")
	}
}

func TestResolveColorNoColorFlag(t *testing.T) {
	if Resolve(false, true, &bytes.Buffer{}) != false {
		t.Fatal("--no-color must disable color")
	}
}

func TestResolveColorNoColorEnv(t *testing.T) {
	t.Setenv("NO_COLOR", "1")
	if Resolve(false, false, &bytes.Buffer{}) != false {
		t.Fatal("NO_COLOR env must disable color")
	}
}

func TestResolveColorNonTTY(t *testing.T) {
	t.Setenv("NO_COLOR", "")
	// io.Writer that's not a tty (a bytes.Buffer) => off
	if Resolve(false, false, &bytes.Buffer{}) != false {
		t.Fatal("non-tty writer must disable color")
	}
}

func TestResolveColorTTYActsOnFile(t *testing.T) {
	// We can't easily fake isatty on bytes.Buffer; this is a smoke test
	// to ensure Resolve doesn't panic when given an *os.File.
	t.Setenv("NO_COLOR", "")
	_ = Resolve(false, false, os.Stderr)
}
```

- [ ] **Step 7.2: 跑测试确认失败（函数不存在）**

- [ ] **Step 7.3: 实现 Resolve**

`internal/render/color.go`：

```go
package render

import (
	"io"
	"os"

	"golang.org/x/term"
)

// Resolve decides whether color output should be enabled. Order of
// precedence (highest first):
//  1. JSON mode → off (avoid polluting pipes)
//  2. --no-color flag → off
//  3. NO_COLOR env var (any non-empty value) → off
//  4. FORCE_COLOR env var → on
//  5. stdout/writer is a TTY → on
//  6. otherwise → off
func Resolve(jsonMode bool, noColorFlag bool, w io.Writer) bool {
	if jsonMode {
		return false
	}
	if noColorFlag {
		return false
	}
	if v := os.Getenv("NO_COLOR"); v != "" {
		return false
	}
	if v := os.Getenv("FORCE_COLOR"); v != "" {
		return true
	}
	if f, ok := w.(*os.File); ok && term.IsTerminal(int(f.Fd())) {
		return true
	}
	return false
}
```

- [ ] **Step 7.4: 跑测试确认通过**

```
go test ./internal/render/ -run TestResolveColor -v
```

- [ ] **Step 7.5: 引 x/term 依赖**

```bash
go get golang.org/x/term
go mod tidy
```

- [ ] **Step 7.6: Commit**

```bash
git add internal/render/color.go internal/render/color_test.go go.mod go.sum
git status --short
git diff --staged --stat
git commit -m "Add color detection (isatty + NO_COLOR + FORCE_COLOR + flags)"
```

---

### Task 8: `internal/render/boxed.go` — 圆角边框表格

**Owner:** cloud

**Files:**
- Create: `internal/render/boxed.go`
- Modify: `internal/render/types.go`（在 defaultRenderer 上挂方法）

- [ ] **Step 8.1: 写失败 test (golden snapshot 风格)**

`internal/render/boxed_test.go`：

```go
package render

import (
	"bytes"
	"strings"
	"testing"
)

func TestRenderAggregateBoxedHasAllColumns(t *testing.T) {
	rows := []AggregateRow{
		{
			Bucket: "2026-05-19", Models: []string{"claude-opus-4-7"},
			InputTokens: 1200, OutputTokens: 45000, CacheCreateTokens: 8000,
			CacheReadTokens: 420000, TotalTokens: 474200, Cost: 18.83,
		},
	}
	var buf bytes.Buffer
	if err := New().RenderAggregate(&buf, "daily", rows, Options{}); err != nil {
		t.Fatal(err)
	}
	out := buf.String()
	for _, want := range []string{"DATE", "MODELS", "INPUT", "OUTPUT", "CACHE", "TOTAL", "COST", "2026-05-19", "claude-opus-4-7", "$18.83"} {
		if !strings.Contains(out, want) {
			t.Errorf("missing %q in output:\n%s", want, out)
		}
	}
}

func TestRenderAggregateBoxedHasTotalsRow(t *testing.T) {
	rows := []AggregateRow{
		{Bucket: "2026-05-19", InputTokens: 100, OutputTokens: 200, TotalTokens: 300, Cost: 1.0},
		{Bucket: "2026-05-20", InputTokens: 50, OutputTokens: 150, TotalTokens: 200, Cost: 0.5},
	}
	var buf bytes.Buffer
	if err := New().RenderAggregate(&buf, "daily", rows, Options{}); err != nil {
		t.Fatal(err)
	}
	out := buf.String()
	if !strings.Contains(out, "TOTAL") {
		t.Fatalf("expected TOTAL summary row, got:\n%s", out)
	}
	// totals should reflect sums (300+200=500 total tokens, 1.5 cost)
	if !strings.Contains(out, "$1.50") {
		t.Errorf("expected $1.50 in totals row, got:\n%s", out)
	}
}

func TestRenderAggregateBoxedEmpty(t *testing.T) {
	var buf bytes.Buffer
	if err := New().RenderAggregate(&buf, "daily", nil, Options{}); err != nil {
		t.Fatal(err)
	}
	if !strings.Contains(buf.String(), "no data in range") {
		t.Fatalf("expected empty-range message, got:\n%s", buf.String())
	}
}

func TestRenderAggregateBoxedBreakdownNests(t *testing.T) {
	rows := []AggregateRow{
		{
			Bucket: "2026-05-19", Models: []string{"claude", "gpt"},
			TotalTokens: 300, Cost: 1.0,
			Breakdown: []ModelBreakdown{
				{Model: "claude", TotalTokens: 200, Cost: 0.7},
				{Model: "gpt", TotalTokens: 100, Cost: 0.3},
			},
		},
	}
	var buf bytes.Buffer
	if err := New().RenderAggregate(&buf, "daily", rows, Options{Breakdown: true}); err != nil {
		t.Fatal(err)
	}
	if !strings.Contains(buf.String(), "└─ claude") {
		t.Fatalf("expected └─ claude breakdown row, got:\n%s", buf.String())
	}
}
```

- [ ] **Step 8.2: 跑测试确认失败**

- [ ] **Step 8.3: 实现 RenderAggregate**

`internal/render/boxed.go`：

```go
package render

import (
	"fmt"
	"io"
	"strings"

	"github.com/jedib0t/go-pretty/v6/table"
	"github.com/jedib0t/go-pretty/v6/text"
)

func (d defaultRenderer) RenderAggregate(w io.Writer, kind string, rows []AggregateRow, opts Options) error {
	if opts.JSON {
		return d.renderAggregateJSON(w, kind, rows)
	}
	if len(rows) == 0 {
		fmt.Fprintln(w, "(no data in range)")
		return nil
	}
	t := table.NewWriter()
	t.SetOutputMirror(w)
	t.SetStyle(table.StyleRounded)
	firstCol := strings.ToUpper(kind[:1]) + kind[1:] // "Daily" -> first column header "Date"
	if kind == "weekly" {
		firstCol = "Week"
	} else if kind == "monthly" {
		firstCol = "Month"
	} else {
		firstCol = "Date"
	}
	t.AppendHeader(table.Row{firstCol, "Models", "Input", "Output", "Cache Crt.", "Cache Read", "Total", "Cost"})

	colorize := func(c text.Color, s string) string {
		if !opts.Color { return s }
		return c.Sprint(s)
	}

	var sumIn, sumOut, sumCC, sumCR, sumTotal int64
	var sumCost float64
	for _, r := range rows {
		t.AppendRow(table.Row{
			r.Bucket,
			colorize(text.FgCyan, joinList(r.Models)),
			colorize(text.FgYellow, fmtInt(r.InputTokens)),
			colorize(text.FgYellow, fmtInt(r.OutputTokens)),
			colorize(text.FgYellow, fmtInt(r.CacheCreateTokens)),
			colorize(text.FgYellow, fmtInt(r.CacheReadTokens)),
			colorize(text.FgYellow, fmtInt(r.TotalTokens)),
			colorize(text.FgRed, fmtCost(r.Cost)),
		})
		if opts.Breakdown {
			for _, b := range r.Breakdown {
				t.AppendRow(table.Row{
					"└─ " + b.Model, "",
					fmtInt(b.InputTokens), fmtInt(b.OutputTokens),
					fmtInt(b.CacheCreateTokens), fmtInt(b.CacheReadTokens),
					fmtInt(b.TotalTokens), fmtCost(b.Cost),
				})
			}
		}
		sumIn += r.InputTokens
		sumOut += r.OutputTokens
		sumCC += r.CacheCreateTokens
		sumCR += r.CacheReadTokens
		sumTotal += r.TotalTokens
		sumCost += r.Cost
	}
	t.AppendSeparator()
	t.AppendFooter(table.Row{
		"TOTAL", "",
		fmtInt(sumIn), fmtInt(sumOut),
		fmtInt(sumCC), fmtInt(sumCR),
		fmtInt(sumTotal), fmtCost(sumCost),
	})
	t.Render()
	return nil
}

func fmtInt(n int64) string {
	if n >= 1_000_000 {
		return fmt.Sprintf("%.2fM", float64(n)/1_000_000)
	}
	if n >= 1_000 {
		return fmt.Sprintf("%.1fK", float64(n)/1_000)
	}
	return fmt.Sprintf("%d", n)
}

func fmtCost(c float64) string { return fmt.Sprintf("$%.2f", c) }

func joinList(s []string) string {
	if len(s) == 0 { return "-" }
	return strings.Join(s, ", ")
}
```

实现 `RenderSessions` / `RenderBlocks` 用同一 helper，列分别为：
- Sessions: `Session | Project | Models | Input | Output | Cache Crt. | Cache Read | Total | Cost`
- Blocks: `Period | Models | Input | Output | Cache Crt. | Cache Read | Total | Cost | Status`

每个都有同样的 TOTAL（blocks 不要 TOTAL Status 列）/ empty / breakdown 处理。

- [ ] **Step 8.4: 跑测试确认通过**

```
go test ./internal/render/ -run TestRenderAggregateBoxed -v
go test ./internal/render/ -v
```

- [ ] **Step 8.5: Commit**

```bash
git add internal/render/boxed.go internal/render/boxed_test.go internal/render/types.go
git status --short
git diff --staged --stat
git commit -m "Implement rounded-box table rendering with totals and breakdown"
```

---

### Task 9: `internal/render/json.go` — ccusage camelCase JSON

**Owner:** cloud

**Files:**
- Create: `internal/render/json.go`
- Create: `internal/render/json_test.go`

- [ ] **Step 9.1: 写失败 test**

```go
package render

import (
	"bytes"
	"encoding/json"
	"testing"
)

func TestRenderAggregateJSONSchema(t *testing.T) {
	rows := []AggregateRow{
		{
			Bucket: "2026-05-19", Models: []string{"claude"},
			InputTokens: 100, OutputTokens: 200, CacheCreateTokens: 10,
			CacheReadTokens: 50, TotalTokens: 360, Cost: 1.23,
		},
	}
	var buf bytes.Buffer
	if err := New().RenderAggregate(&buf, "daily", rows, Options{JSON: true}); err != nil {
		t.Fatal(err)
	}
	var got struct {
		Daily []struct {
			Date                string  `json:"date"`
			ModelsUsed          []string `json:"modelsUsed"`
			InputTokens         int64   `json:"inputTokens"`
			OutputTokens        int64   `json:"outputTokens"`
			CacheCreationTokens int64   `json:"cacheCreationTokens"`
			CacheReadTokens     int64   `json:"cacheReadTokens"`
			TotalTokens         int64   `json:"totalTokens"`
			TotalCost           float64 `json:"totalCost"`
		} `json:"daily"`
		Totals struct {
			InputTokens  int64   `json:"inputTokens"`
			OutputTokens int64   `json:"outputTokens"`
			TotalCost    float64 `json:"totalCost"`
		} `json:"totals"`
	}
	if err := json.Unmarshal(buf.Bytes(), &got); err != nil {
		t.Fatalf("unmarshal: %v\n%s", err, buf.String())
	}
	if len(got.Daily) != 1 || got.Daily[0].Date != "2026-05-19" {
		t.Fatalf("daily row wrong: %+v", got.Daily)
	}
	if got.Daily[0].TotalCost != 1.23 {
		t.Errorf("totalCost: got %v, want 1.23", got.Daily[0].TotalCost)
	}
	if got.Totals.InputTokens != 100 {
		t.Errorf("totals.inputTokens: got %d, want 100", got.Totals.InputTokens)
	}
}

func TestRenderAggregateJSONEmpty(t *testing.T) {
	var buf bytes.Buffer
	if err := New().RenderAggregate(&buf, "daily", nil, Options{JSON: true}); err != nil {
		t.Fatal(err)
	}
	// Empty JSON should still be valid + parseable
	var got map[string]any
	if err := json.Unmarshal(buf.Bytes(), &got); err != nil {
		t.Fatalf("unmarshal empty: %v\n%s", err, buf.String())
	}
}

func TestRenderSessionsJSONSchema(t *testing.T) {
	rows := []SessionRow{
		{SessionID: "abc", ProjectPath: "/path", Models: []string{"claude"},
		 InputTokens: 100, TotalTokens: 100, Cost: 1.0},
	}
	var buf bytes.Buffer
	if err := New().RenderSessions(&buf, rows, Options{JSON: true}); err != nil {
		t.Fatal(err)
	}
	if !bytes.Contains(buf.Bytes(), []byte(`"sessionId":"abc"`)) &&
	   !bytes.Contains(buf.Bytes(), []byte(`"sessionId": "abc"`)) {
		t.Fatalf("expected sessionId field, got:\n%s", buf.String())
	}
}
```

- [ ] **Step 9.2: 跑测试确认失败**

- [ ] **Step 9.3: 实现 RenderXxx JSON 分支**

`internal/render/json.go`：

```go
package render

import (
	"encoding/json"
	"io"
)

type aggregateJSONRow struct {
	Date                string             `json:"date,omitempty"`
	Week                string             `json:"week,omitempty"`
	Month               string             `json:"month,omitempty"`
	ModelsUsed          []string           `json:"modelsUsed"`
	InputTokens         int64              `json:"inputTokens"`
	OutputTokens        int64              `json:"outputTokens"`
	CacheCreationTokens int64              `json:"cacheCreationTokens"`
	CacheReadTokens     int64              `json:"cacheReadTokens"`
	TotalTokens         int64              `json:"totalTokens"`
	TotalCost           float64            `json:"totalCost"`
	ModelBreakdowns     []breakdownJSON    `json:"modelBreakdowns,omitempty"`
}

type breakdownJSON struct {
	Model               string  `json:"model"`
	InputTokens         int64   `json:"inputTokens"`
	OutputTokens        int64   `json:"outputTokens"`
	CacheCreationTokens int64   `json:"cacheCreationTokens"`
	CacheReadTokens     int64   `json:"cacheReadTokens"`
	TotalTokens         int64   `json:"totalTokens"`
	TotalCost           float64 `json:"totalCost"`
}

type totalsJSON struct {
	InputTokens         int64   `json:"inputTokens"`
	OutputTokens        int64   `json:"outputTokens"`
	CacheCreationTokens int64   `json:"cacheCreationTokens"`
	CacheReadTokens     int64   `json:"cacheReadTokens"`
	TotalTokens         int64   `json:"totalTokens"`
	TotalCost           float64 `json:"totalCost"`
}

func (d defaultRenderer) renderAggregateJSON(w io.Writer, kind string, rows []AggregateRow) error {
	outRows := make([]aggregateJSONRow, 0, len(rows))
	var t totalsJSON
	for _, r := range rows {
		row := aggregateJSONRow{
			ModelsUsed:          r.Models,
			InputTokens:         r.InputTokens,
			OutputTokens:        r.OutputTokens,
			CacheCreationTokens: r.CacheCreateTokens,
			CacheReadTokens:     r.CacheReadTokens,
			TotalTokens:         r.TotalTokens,
			TotalCost:           r.Cost,
		}
		switch kind {
		case "weekly":
			row.Week = r.Bucket
		case "monthly":
			row.Month = r.Bucket
		default:
			row.Date = r.Bucket
		}
		for _, b := range r.Breakdown {
			row.ModelBreakdowns = append(row.ModelBreakdowns, breakdownJSON{
				Model: b.Model, InputTokens: b.InputTokens, OutputTokens: b.OutputTokens,
				CacheCreationTokens: b.CacheCreateTokens, CacheReadTokens: b.CacheReadTokens,
				TotalTokens: b.TotalTokens, TotalCost: b.Cost,
			})
		}
		outRows = append(outRows, row)
		t.InputTokens += r.InputTokens
		t.OutputTokens += r.OutputTokens
		t.CacheCreationTokens += r.CacheCreateTokens
		t.CacheReadTokens += r.CacheReadTokens
		t.TotalTokens += r.TotalTokens
		t.TotalCost += r.Cost
	}
	envelope := map[string]any{
		kind:     outRows,
		"totals": t,
	}
	enc := json.NewEncoder(w)
	enc.SetIndent("", "  ")
	return enc.Encode(envelope)
}

// RenderSessions / RenderBlocks JSON 分支同理：顶层 key 分别是 "sessions"
// 和 "blocks"，row schema 见 spec §Phase B.4。
```

写完 RenderSessions / RenderBlocks 的 JSON 分支。

- [ ] **Step 9.4: 跑测试确认通过**

- [ ] **Step 9.5: Commit**

```bash
git add internal/render/json.go internal/render/json_test.go
git status --short
git diff --staged --stat
git commit -m "Implement ccusage-aligned camelCase JSON renderer"
```

---

### Task 10: `cli/aggregate.go` 切换到 render 包

**Owner:** star（**注意**：cloud 完成 Task 6-9 之后再开工，避免空指针引入）

**Files:**
- Modify: `cmd/tm/cli/aggregate.go`
- Test: `cmd/tm/cli/aggregate_test.go`

- [ ] **Step 10.1: 改 RunAggregate 输出层**

把 `tabwriter` 部分替换为 render 调用：

```go
import "github.com/tt-a1i/tokenmeter/internal/render"

// ... after groupBy + sorting ...
rows := make([]render.AggregateRow, 0, len(keys))
for _, k := range keys {
	g := groups[k]
	row := render.AggregateRow{
		Bucket:            k,
		Models:            g.models,
		InputTokens:       g.input,
		OutputTokens:      g.output,
		CacheCreateTokens: g.cacheCreate,
		CacheReadTokens:   g.cacheRead,
		TotalTokens:       g.input + g.output + g.cacheCreate + g.cacheRead,
		Cost:              g.cost,
	}
	if a.Shared.Breakdown {
		for model, st := range g.perModel {
			row.Breakdown = append(row.Breakdown, render.ModelBreakdown{
				Model: model, InputTokens: st.Input, OutputTokens: st.Output,
				CacheCreateTokens: st.CacheCreate, CacheReadTokens: st.CacheRead,
				TotalTokens: st.Input + st.Output + st.CacheCreate + st.CacheRead,
				Cost: st.Cost,
			})
		}
		sort.Slice(row.Breakdown, func(i, j int) bool {
			return row.Breakdown[i].Cost > row.Breakdown[j].Cost
		})
	}
	rows = append(rows, row)
}

kind := bucketKind(a.Bucket)
opts := render.Options{
	JSON:      a.Shared.JSON,
	Breakdown: a.Shared.Breakdown,
	Color:     render.Resolve(a.Shared.JSON, a.Shared.NoColor, w),
}
return render.New().RenderAggregate(w, kind, rows, opts)
```

加 helper：
```go
func bucketKind(b Bucket) string {
	switch b {
	case BucketWeekly: return "weekly"
	case BucketMonthly: return "monthly"
	default: return "daily"
	}
}
```

- [ ] **Step 10.2: 更新已有测试以匹配新输出**

把 Task 1 中 `TestRunAggregateOrderDesc` 等 test 改为：要么用 `--json` 输出做断言（更稳），要么验证 bucket 顺序而非具体格式。

- [ ] **Step 10.3: 跑全包测试**

```
go test ./cmd/tm/cli/ -v
go build -o tm ./cmd/tm/ && ./tm daily | head -5
```

- [ ] **Step 10.4: Commit**

```bash
git add cmd/tm/cli/aggregate.go cmd/tm/cli/aggregate_test.go
git status --short
git diff --staged --stat
git commit -m "Switch RunAggregate output to render package"
```

---

### Task 11: `cli/session.go` 切换到 render 包

**Owner:** star

**Files:**
- Modify: `cmd/tm/cli/session.go`
- Test: `cmd/tm/cli/session_test.go`

仿 Task 10：把 RunSession 输出替换为 `render.New().RenderSessions(...)`。SessionRow 的 ProjectPath / LastActivity 字段：
- ProjectPath 从 storage.SessionRow.Cwd 读取（如果 ListUsageForBlocksFiltered 不带 cwd，则增加一个 storage 方法 `ListSessionsFiltered` 返回 SessionID → Cwd 映射）；最小变更思路：单独 `db.ListSessions(ctx)` 取 sessionID→cwd 字典，在 cli 层 join
- LastActivity 从 entries 里同 sessionID 的 max Timestamp 计算

- [ ] **Step 11.1**: 写测试 → 失败 → 实现 → 测试通过 → commit

提交 message："Switch RunSession output to render package; surface project path"

---

### Task 12: `cli/blocks.go` 切换到 render 包

**Owner:** star

**Files:**
- Modify: `cmd/tm/cli/blocks.go`
- Test: `cmd/tm/cli/blocks_test.go`

仿 Task 10。BlockRow.Status = "ACTIVE" / "closed" / "gap" 依据 b.IsActive / b.IsGap。

- [ ] **Step 12.1**: 写测试 → 失败 → 实现 → 测试通过 → commit

提交 message："Switch RunBlocks output to render package"

---

**Phase B 边界**：Reviewer 检查 Tasks 6-12。重点：
1. `tm daily / weekly / monthly / session / blocks` 视觉与 ccusage 一致
2. `--json` 字段 camelCase + 顶层 envelope
3. `--breakdown` 嵌套显示
4. `--no-color` / pipe / `NO_COLOR` 三种关色路径
5. lint 0 issues

---

## Phase C — 闭区间 + 空数据（Task 13）

### Task 13: `--until` +24h 闭区间

**Owner:** cloud

**Files:**
- Modify: `cmd/tm/cli/aggregate.go`
- Test: `cmd/tm/cli/aggregate_test.go`

- [ ] **Step 13.1: 写失败 test**

```go
func TestParseDateFlagUntilClosedInterval(t *testing.T) {
	// "20260520" should yield 2026-05-21 00:00 (so query uses <= 5/21 i.e.
	// includes the entirety of 5/20).
	got, err := parseDateFlagUntil("20260520")
	if err != nil { t.Fatal(err) }
	want := time.Date(2026, 5, 21, 0, 0, 0, 0, time.UTC)
	if !got.Equal(want) {
		t.Errorf("parseDateFlagUntil(20260520) = %v, want %v", got, want)
	}
}
```

- [ ] **Step 13.2: 跑测试确认失败（函数不存在）**

- [ ] **Step 13.3: 实现 parseDateFlagUntil**

`cmd/tm/cli/aggregate.go`：

```go
// parseDateFlagUntil parses a YYYYMMDD value as ccusage does: inclusive
// of the given day. It returns the start of the *next* day, so callers
// that use < comparisons get closed-interval semantics.
func parseDateFlagUntil(s string) (time.Time, error) {
	t, err := parseDateFlag(s)
	if err != nil { return time.Time{}, err }
	if t.IsZero() { return t, nil }
	return t.Add(24 * time.Hour), nil
}
```

把 RunAggregate / RunSession / RunBlocks 里 `until, err := parseDateFlag(a.Shared.Until)` 全部改成 `until, err := parseDateFlagUntil(a.Shared.Until)`。

- [ ] **Step 13.4: 跑测试确认通过**

- [ ] **Step 13.5: 写集成 test — 5/20 数据 in `--until 20260520`**

```go
func TestRunAggregateUntilInclusive(t *testing.T) {
	entries := []storage.TokenUsageEntry{
		{SessionID: "s", Timestamp: mustTime("2026-05-20T23:59:00Z"), InputTokens: 100, Model: "x"},
	}
	args := AggregateArgs{Shared: Shared{Until: "20260520", JSON: true}, Bucket: BucketDaily}
	var buf bytes.Buffer
	if err := RunAggregate(context.Background(), &buf, args, stubLoader{entries: entries}); err != nil {
		t.Fatal(err)
	}
	if !strings.Contains(buf.String(), "2026-05-20") {
		t.Fatalf("expected 2026-05-20 in output (closed --until), got:\n%s", buf.String())
	}
}
```

- [ ] **Step 13.6: Commit**

```bash
git add cmd/tm/cli/aggregate.go cmd/tm/cli/aggregate_test.go cmd/tm/cli/session.go cmd/tm/cli/blocks.go
git status --short
git diff --staged --stat
git commit -m "Make --until closed-interval (matches ccusage)"
```

---

## Phase D — 性能 profile（Task 14）

### Task 14: `--cpu-profile` hidden flag + baseline 报告

**Owner:** cloud

**Files:**
- Modify: `cmd/tm/cli/cli.go`
- Modify: `cmd/tm/main.go`
- Create: `docs/perf/v1.0.1-baseline.md`

- [ ] **Step 14.1: 加 hidden flag**

`cmd/tm/cli/cli.go`，在 ParseShared 中追加：

```go
fs.StringVar(&s.CPUProfile, "cpu-profile", "", "write CPU profile to file (hidden)")
```

Shared struct 加 `CPUProfile string`。

- [ ] **Step 14.2: 在 main.go 启停**

`cmd/tm/main.go::runCLIDispatch`：

```go
if cmd.Shared.CPUProfile != "" {
	f, err := os.Create(cmd.Shared.CPUProfile)
	if err != nil { return fmt.Errorf("create cpu profile: %w", err) }
	defer f.Close()
	if err := pprof.StartCPUProfile(f); err != nil {
		return fmt.Errorf("start cpu profile: %w", err)
	}
	defer pprof.StopCPUProfile()
}
```

import `runtime/pprof`、`os`。

- [ ] **Step 14.3: 跑 baseline**

```bash
go build -o tm ./cmd/tm
./tm daily --cpu-profile /tmp/v1.0.1-baseline.pprof > /dev/null
go tool pprof -top -cum /tmp/v1.0.1-baseline.pprof | head -30 > /tmp/top.txt
```

- [ ] **Step 14.4: 写 baseline 报告**

`docs/perf/v1.0.1-baseline.md`：

```markdown
# v1.0.1 性能基线

**测试环境:**
- macOS 25.x · Apple Silicon
- DB: ~/.agmon/data/agmon.db (\<sessions\>, \<rows\>, \<size\>)
- 命令: `./tm daily --cpu-profile /tmp/v1.0.1-baseline.pprof > /dev/null`

**总耗时（time）:** 约 \<measured\> ms / \<n\> 次取中位数

**Top 10 累计耗时（go tool pprof -top -cum）:**

\`\`\`
<paste pprof output>
\`\`\`

**结论:**
- \<top hotspot 1 与 v1.0.2 优化候选 1\>
- \<top hotspot 2 与 v1.0.2 优化候选 2\>
- 短期目标（v1.0.2 commit-or-defer 决定）:
  - 若 SQLite scan 占大头 → 加 (timestamp DESC) 索引
  - 若 pricing.LoadMap 占大头 → sync.Once 单例
  - 若 daemon socket dial 占大头 → 取消 dial 路径（CLI 直读 SQLite）
```

填空：把 measure 出来的数据写入。

- [ ] **Step 14.5: Commit**

```bash
git add cmd/tm/cli/cli.go cmd/tm/main.go docs/perf/v1.0.1-baseline.md
git status --short
git diff --staged --stat
git commit -m "Add --cpu-profile hidden flag and v1.0.1 baseline report"
```

---

**Phase D 边界**：Reviewer 检查 baseline 报告是否完整。

---

## Phase E — 文档 + 发布（Task 15）

### Task 15: CHANGELOG + MIGRATION + --help 文本 + release stamp

**Owner:** cloud

**Files:**
- Modify: `CHANGELOG.md`
- Modify: `docs/MIGRATION-v1.0.md`
- Modify: `cmd/tm/main.go::printHelp` (如有 daily/weekly/etc 描述需校准)

- [ ] **Step 15.1: 写 CHANGELOG v1.0.1 段**

`CHANGELOG.md` 顶部插入：

```markdown
## v1.0.1 — YYYY-MM-DD

### Added
- `--json` 实际生效（daily/weekly/monthly/session/blocks），字段 camelCase + 顶层 envelope（与 ccusage v20 对齐）
- `--breakdown` 在 daily/weekly/monthly/session 中实际生效，输出 model-level 嵌套子行
- `--order desc` 倒序输出
- `--mode auto|calculate|display` 在所有 subcommand 生效，作用于 pricing
- `--timezone <IANA>` 按指定时区分桶
- `--project <path>` 接 sessions.cwd 过滤
- `--no-color` 实际生效；自动 isatty 检测 + 支持 `NO_COLOR` / `FORCE_COLOR` 环境变量
- 表格输出对齐 ccusage：8 列 + 圆角边框 + TOTAL 汇总行 + 颜色（COST 红 / TOKENS 黄 / Models 青）
- 空范围输出 `(no data in range)`
- `--cpu-profile <path>` hidden flag（开发者用）

### Changed
- `tm`（无参）默认执行 `daily`，与 ccusage 一致（之前是打印 help）
- `--until YYYYMMDD` 改为闭区间（包含当天全部数据），与 ccusage 一致

### Performance
- v1.0.1 baseline 报告：`docs/perf/v1.0.1-baseline.md`。当前 `tm daily` 在 1587 sessions / 575MB DB 上约 800ms；优化承诺留 v1.0.2
```

实际发布时把 YYYY-MM-DD 改为当天日期。

- [ ] **Step 15.2: 写 MIGRATION v1.0.1 段**

`docs/MIGRATION-v1.0.md` 加 section：

```markdown
## v1.0.1 changes

### `--until` 语义变化（潜在 breaking）

v1.0.0：`--until 20260520` 等同 `< 2026-05-20 00:00 UTC`（开区间），**不含** 5/20 数据
v1.0.1：`--until 20260520` 等同 `< 2026-05-21 00:00 UTC`（闭区间），**包含** 5/20 数据

如果 v1.0.0 脚本依赖旧行为，请把日期减一天。

### JSON schema

v1.0.0：`--json` 无效（输出仍是表格）
v1.0.1：`--json` 顶层 envelope，字段 camelCase。详见 `docs/superpowers/specs/2026-05-20-v1.0.1-ccusage-alignment-part1-design.md` §Phase B.4。
```

- [ ] **Step 15.3: 校准 `--help` 文本**

跑：
```bash
./tm help | less
./tm daily --help
```
确认所有命令的 help 文本与实际行为一致。改 `cmd/tm/main.go::printHelp` 中 daily/weekly/monthly/session/blocks 一行 description 反映新行为（无须重写整段）。

- [ ] **Step 15.4: 全套 sanity**

```bash
go test ./...
golangci-lint run
go build -o tm ./cmd/tm
./tm daily | head -10
./tm daily --json | jq '.totals'
./tm daily --breakdown | head -20
./tm daily --order desc | head -5
./tm daily --until 20260520 | grep 2026-05-20
./tm daily --timezone Asia/Shanghai | head -5
./tm daily --no-color | head -5
./tm | head -5  # default to daily
./tm blocks --active
```

每条都要看输出正常。

- [ ] **Step 15.5: Commit**

```bash
git add CHANGELOG.md docs/MIGRATION-v1.0.md cmd/tm/main.go
git status --short
git diff --staged --stat
git commit -m "Document v1.0.1 changes (CHANGELOG, MIGRATION, help text)"
```

---

**Phase E 完成 = v1.0.1 ready to tag**。

---

## 验收 Gate（reviewer 最终检查）

1. `go test ./...` 全绿
2. `golangci-lint run` 0 issues
3. 14 个 `./tm` 手测命令全部输出符合 spec §验收标准
4. `docs/perf/v1.0.1-baseline.md` 含完整 top10 + 决策
5. CHANGELOG / MIGRATION 段落齐全
6. 当天日期写入 CHANGELOG `v1.0.1 — YYYY-MM-DD`

通过后：
```bash
git tag -a v1.0.1 -m "v1.0.1 — ccusage alignment Part 1"
git push origin main v1.0.1
```

CI 跑通 → goreleaser 发 6 平台 binary。

---

## 工程纪律（与 v1.0 一致）

- `git add` 显式路径，**不**用 `git add .` / `-A`
- `git status --short` 二验，`git diff --staged --stat` 三验
- commit message 无 Claude 品牌
- 不 skip hooks（`--no-verify`）、不 force push、不改 git config
- 包内串行，包外并发：
  - `cmd/tm/cli/aggregate.go` 由 star 独占
  - `cmd/tm/cli/session.go` 由 star 独占
  - `cmd/tm/cli/blocks.go` 由 star 独占
  - `cmd/tm/cli/statusline.go` 由 cloud 独占
  - `cmd/tm/cli/router.go` + `cmd/tm/main.go` 由 star 独占
  - `internal/render/` 由 cloud 独占
  - `CHANGELOG.md` / `docs/` 由 cloud 独占
- Task 10/11/12 切换 render 必须在 Task 8/9（render 包成型）之后开始
- 跨包改动前先通过 reviewer 在 Phase 边界 gate

---

## 总计

15 个 task。预计 1-2 天双 worker 并行完工。CI / goreleaser 已就绪（v1.0.0 走通过），release 流程不变。
